# US-018 编辑设备资料并查询物联网卡

## POINT-001 详情页字段与展示规则

## [P1] 验证详情页字段构成与数据来源
- 测试类型
  - 功能
- 前置条件
  1. P-201备注"牧场一号牛"，联系人"张三"，电话+8613800138000，归属"天河区"，ICCID 8986001234567890123，服务到期2027-06-30，型号TL-100，创建时间2026-01-15。
- 测试步骤
  1. 进入P-201设备详情查看态。
  2. 逐项核对九个字段。
- 预期结果
  1. 展示Device ID、ICCID、Expiration Date（2027-06-30）、Device Model（TL-100）、Group（天河区）、Device Name（牧场一号牛）、Contact（张三）、Phone Number（+8613800138000）、Location Interval、Created At（2026-01-15）。
  2. 各字段值与来源一致，日期按YYYY-MM-DD展示。

## [P2] 验证只读字段不可修改
- 测试类型
  - 功能
- 前置条件
  1. 进入P-201详情编辑态。
- 测试步骤
  1. 尝试修改Device ID、ICCID、Expiration Date、Device Model、Created At。
  2. 查看各字段编辑能力。
- 预期结果
  1. 五个只读字段均不可修改。
  2. Device Model展示实际硬件型号，不由名称或前端选项反推。

## [P3][反向] 验证只读字段缺失统一显示短横线
- 测试类型
  - 功能
- 前置条件
  1. P-203服务端未维护服务到期时间，ICCID无关联卡记录。
- 测试步骤
  1. 进入P-203详情查看态。
  2. 查看Expiration Date与ICCID行展示。
- 预期结果
  1. 两个字段行保留。
  2. 缺失值统一显示"-"，不显示0或伪造日期，客户端不补默认日期。

## [P2] 验证可编辑空字段查看态展示短横线
- 测试类型
  - 功能
- 前置条件
  1. P-204的Device Name、Contact、Phone Number均为空。
- 测试步骤
  1. 查看P-204详情查看态三个字段。
  2. 返回设备列表查看P-204卡片第一行。
- 预期结果
  1. 查看态按UI显示"-"。
  2. 列表标题按"设备名称（备注）优先、Device ID兜底"展示卡号P-204。

## [P1] 验证无确认值时定位频率显示请选择
- 测试类型
  - 功能
- 前置条件
  1. P-205刚绑定，平台无该设备历史成功UPLOAD记录，无设置中目标。
- 测试步骤
  1. 查看P-205详情查看态Location Interval。
  2. 进入编辑态打开频率弹层查看选择框。
- 预期结果
  1. 查看态显示"请选择定位频率"。
  2. 弹层选择框默认显示"请选择定位频率"，不预选目标，不用-、技术状态名或10分钟默认值替代。

## [P2] 验证详情请求失败可恢复重试
- 测试类型
  - 可靠性
- 前置条件
  1. mock设备详情接口返回失败。
- 测试步骤
  1. 进入P-201详情。
  2. 查看页面展示。
  3. 恢复接口后重试进入。
- 预期结果
  1. 保留可恢复页面状态并展示可重试原因。
  2. 不展示残缺或伪造数据。
  3. 重试后正常展示详情。

## [P3][反向] 验证服务端重新校验可见范围
- 测试类型
  - 安全性
- 前置条件
  1. alice01已进入P-206详情页。
  2. 另一端解除alice01与P-206关系。
- 测试步骤
  1. 在详情页执行保存操作。
  2. 抓包构造对P-206的资料保存请求重放。
- 预期结果
  1. 服务端按当前登录账户与Device ID重新校验。
  2. 设备已不可见时保存被拒绝，不依赖前端按钮控制，不产生越权写入。
