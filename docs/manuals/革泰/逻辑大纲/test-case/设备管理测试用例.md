# 革泰 APP 设备管理测试用例汇总

> 依据：革泰 APP 设备管理逻辑点梳理 plan.md（v2.0，2026-09-21）。未定稿的文案或交互以「待确认/记录实际表现」方式标注，不作为硬编码断言。

# US-015 设备列表筛选与快捷操作


## POINT-001 状态统计计数规则

## [P1] 验证设备列表四种状态筛选及计数正确
- 测试类型
  - 功能
- 前置条件
  1. 个人账号alice01可见5台设备：P-001在线、P-002在线、P-003离线、P-004初始化未连接、P-005告警且在线。
- 测试步骤
  1. alice01登录APP进入设备列表。
  2. 查看顶部"全部/在线/离线/告警"四个筛选项的数量。
  3. 依次点击"在线""离线""告警""全部"筛选并查看列表内容。
- 预期结果
  1. 全部=5、在线=3、离线=2、告警=1，满足在线数+离线数=设备总数。
  2. "在线"列表展示P-001、P-002、P-005；"离线"列表展示P-003、P-004；"告警"列表仅展示P-005。
  3. "全部"恢复展示5台设备，各筛选数量不变。

## [P1] 验证未连接设备统一按离线展示筛选与计数
- 测试类型
  - 功能
- 前置条件
  1. 设备P-004初始化绑定后尚无任何数据上报，未完成过有效下发与通信建立。
- 测试步骤
  1. alice01登录APP进入设备列表。
  2. 查看P-004卡片状态图标与第三行文案。
  3. 点击"离线"筛选查看P-004是否命中。
  4. 查看顶部筛选项数量。
- 预期结果
  1. 设备列表正常展示，P-004出现在列表中。
  2. P-004卡片状态图标为离线灰色，第三行展示"离线，无法下发指令"。
  3. "离线"筛选可命中P-004，"全部"中也可找到。
  4. 顶部不存在"未连接"筛选项，P-004计入离线数，不额外增加第三类基础状态。

## [P2] 验证告警加在线设备同时计入在线与告警
- 测试类型
  - 功能
- 前置条件
  1. 设备P-005当前在线且触发未处理告警。
- 测试步骤
  1. 进入设备列表查看P-005卡片。
  2. 点击"在线"筛选。
  3. 点击"告警"筛选。
- 预期结果
  1. P-005卡片左侧图标为告警红色并带在线角标，卡片右侧状态优先展示"告警"。
  2. "在线"筛选结果包含P-005。
  3. "告警"筛选结果包含P-005，P-005同时计入在线数与告警数。

## [P2] 验证告警加离线设备计入离线与告警并保留离线角标
- 测试类型
  - 功能
- 前置条件
  1. 设备P-006当前离线且存在未处理告警。
- 测试步骤
  1. 进入设备列表查看P-006卡片图标与第三行。
  2. 点击"离线"筛选。
  3. 点击"告警"筛选。
- 预期结果
  1. P-006图标为告警红色并带离线角标，第三行展示"离线，无法下发指令"。
  2. "离线"筛选结果包含P-006。
  3. "告警"筛选结果包含P-006，P-006同时计入离线数与告警数。

## [P2] 验证多关系可见设备按唯一标识去重计数
- 测试类型
  - 功能
- 前置条件
  1. 企业一级账号owner01绑定P-101并已通过Web分配给二级worker02和worker03。
  2. worker02名下另有机型相同的P-102在线。
- 测试步骤
  1. owner01登录APP查看设备总数与状态数量。
  2. worker02登录APP查看设备总数与状态数量。
  3. worker03登录APP查看设备总数与状态数量。
- 预期结果
  1. owner01列表P-101只出现一条，总数、在线数、离线数、告警数均按设备唯一标识去重。
  2. worker02总数为2（P-101、P-102各计一台）。
  3. worker03总数为1，各端计数不因多条可见关系重复累计。

## [P3][反向] 验证状态筛选一次只选一项
- 测试类型
  - 功能
- 前置条件
  1. 设备列表顶部展示全部/在线/离线/告警四个筛选项。
- 测试步骤
  1. 点击"在线"筛选。
  2. 不取消"在线"直接点击"告警"筛选。
  3. 查看列表结果与筛选选中态。
- 预期结果
  1. "在线"筛选生效，列表仅展示在线设备。
  2. 切换后仅"告警"为选中态，"在线"取消选中。
  3. 列表仅展示告警设备，不出现在线∩告警的叠加结果。

## [P3][反向] 验证默认离线设备在全部与离线筛选中均可找到
- 测试类型
  - 功能
- 前置条件
  1. 设备P-003为离线状态，设备P-004为初始化未连接状态。
- 测试步骤
  1. 进入设备列表默认"全部"视图查找P-003与P-004。
  2. 切换"离线"筛选查找P-003与P-004。
- 预期结果
  1. "全部"视图中P-003、P-004均可见。
  2. "离线"筛选中P-003、P-004均可见，无离线设备遗漏在筛选之外。

## [P2] 验证告警为设备卡片状态优先展示项
- 测试类型
  - 功能
- 前置条件
  1. 设备P-005在线且触发告警，同列表存在普通在线设备P-001。
- 测试步骤
  1. 进入设备列表查看P-005与P-001卡片右侧状态。
  2. 触发P-001产生一条未处理告警后刷新列表。
- 预期结果
  1. P-005卡片右侧优先展示"告警"，P-001展示在线状态。
  2. P-001产生告警后卡片右侧优先切换为"告警"，告警数+1。


## POINT-002 筛选搜索与条件组合

## [P1] 验证关键词搜索命中编号名称备注与序列号
- 测试类型
  - 功能
- 前置条件
  1. alice01名下：P-001备注"运牛车A"、P-002序列号SN8866、P-003名称含"cow"、P-004无备注。
- 测试步骤
  1. 在搜索框输入"P-001"并确认。
  2. 清空后输入"运牛车A"并确认。
  3. 清空后输入"SN8866"并确认。
  4. 清空后输入"cow"并确认。
- 预期结果
  1. 列表仅展示设备编号命中"P-001"的设备。
  2. 列表仅展示备注为"运牛车A"的P-001。
  3. 列表仅展示序列号含"SN8866"的P-002。
  4. 列表仅展示名称含"cow"的P-003，四类字段均可作为检索入口。

## [P2] 验证搜索不区分大小写且忽略首尾空格
- 测试类型
  - 功能
- 前置条件
  1. P-001备注为"Cattle-01"。
- 测试步骤
  1. 搜索框输入"  cattle-01  "（首尾各2个空格）并确认。
  2. 清空后输入"CATTLE-01"并确认。
- 预期结果
  1. 首尾空格被忽略，命中备注"Cattle-01"的P-001。
  2. 大小写不敏感，仍命中P-001，两种输入返回一致结果。

## [P2] 验证备注展示与备注检索为同一份业务备注
- 测试类型
  - 功能
- 前置条件
  1. P-001当前备注为"牧场一号牛"。
- 测试步骤
  1. 进入设备详情将备注修改为"牧场二号牛"并保存返回列表。
  2. 在搜索框输入"牧场一号牛"确认。
  3. 清空后输入"牧场二号牛"确认。
- 预期结果
  1. 列表卡片第一行同步展示新备注"牧场二号牛（P-001）"。
  2. 按旧备注"牧场一号牛"检索无结果。
  3. 按新备注"牧场二号牛"检索命中P-001，展示与检索使用同一份备注数据。

## [P1] 验证状态分组关键词三条件取交集
- 测试类型
  - 功能
- 前置条件
  1. alice01名下：P-001在线属"天河区"组、P-002在线属"深圳市"组、P-003离线属"天河区"组。
- 测试步骤
  1. 选择状态"在线"、分组"天河区"，输入关键词"P-001"确认。
  2. 查看列表结果。
  3. 删除关键词保留状态与分组，查看列表。
- 预期结果
  1. 三个条件同时应用。
  2. 列表仅展示同时满足在线、属天河区、编号含P-001的设备，即仅P-001。
  3. 清空关键词后恢复展示天河区的P-001与P-003中的在线设备P-001，状态与分组条件保留。

## [P2] 验证修改一项条件不清空其他条件
- 测试类型
  - 功能
- 前置条件
  1. 已选择状态"在线"、分组"天河区"、关键词"P-0"。
- 测试步骤
  1. 将分组从"天河区"切换为"深圳市"。
  2. 查看状态筛选、关键词输入框与列表。
- 预期结果
  1. 分组切换成功。
  2. 状态仍为"在线"、关键词仍为"P-0"，列表按新分组∩在线∩关键词重算。

## [P3][反向] 验证搜索无结果时展示空态
- 测试类型
  - 功能
- 前置条件
  1. alice01名下存在P-001等设备。
- 测试步骤
  1. 搜索框输入不存在的关键词"nonexistent999"并确认。
  2. 查看列表展示。
- 预期结果
  1. 列表刷新为空。
  2. 展示"暂无数据"空态，不展示任何设备卡片，不报错。

## [P2] 验证分组数量不随状态与关键词减少
- 测试类型
  - 功能
- 前置条件
  1. alice01名下"广东省"组共10台设备（6在线4离线），"浙江省"组共3台。
- 测试步骤
  1. 打开分组选择器记录各组数量。
  2. 选择状态"在线"后再次打开分组选择器查看数量。
  3. 输入关键词"P-1"后再次查看分组数量与顶部状态数量。
- 预期结果
  1. 初始广东省（10）、浙江省（3）。
  2. 选择"在线"后分组括号数量仍为广东省（10）、浙江省（3），不随状态筛选减少。
  3. 关键词不缩小分组括号数量与顶部状态数量，仅缩小列表结果。

## [P3][反向] 验证分组搜索不提供未分组筛选项
- 测试类型
  - 功能
- 前置条件
  1. alice01名下所有设备均已归属真实业务分组。
- 测试步骤
  1. 打开分组选择器，在分组搜索框输入"未分组"。
  2. 查看搜索结果与选择器默认列表。
- 预期结果
  1. 搜索无匹配，展示无匹配提示。
  2. 选择器默认列表只有虚拟"全部分组"与真实业务分组，不存在"未分组"筛选项。

## [P2] 验证列表刷新与整群轨迹入口可用
- 测试类型
  - 功能
- 前置条件
  1. alice01名下"广东省"组有P-001、P-002两台在线设备。
- 测试步骤
  1. 在设备列表点击刷新按钮。
  2. 选择分组"广东省"后点击"整群轨迹"入口。
- 预期结果
  1. 列表重新拉取，设备状态与数量为服务端最新值。
  2. 整群轨迹以当前分组范围设备发起查询，不串入其他分组设备。


## POINT-003 设备卡片展示元素

## [P1] 验证有备注设备卡片第一行展示备注加卡号
- 测试类型
  - 功能
- 前置条件
  1. 设备P-001备注为"运牛车A"。
- 测试步骤
  1. 进入设备列表查看P-001卡片第一行。
  2. 查看卡片是否展示分组名称、字段分隔符或右侧状态文字标签。
- 预期结果
  1. 第一行展示"运牛车A（P-001）"。
  2. 卡片内不展示分组名称、字段分隔符和右侧在线/离线/告警文字标签。

## [P1] 验证无备注设备卡片第一行直接展示卡号
- 测试类型
  - 功能
- 前置条件
  1. 设备P-002未设置备注。
- 测试步骤
  1. 进入设备列表查看P-002卡片第一行。
- 预期结果
  1. 第一行直接展示卡号"P-002"，不展示"（卡号）"括号结构，不新增备注唯一性限制。

## [P2] 验证有效电量展示电池图标电量与最近更新时间
- 测试类型
  - 功能
- 前置条件
  1. P-001在线，电量85%，接口返回"最近更新于"时间2026-09-22 10:30。
  2. P-003离线，电量60%，有历史电量返回。
- 测试步骤
  1. 查看P-001卡片第二行。
  2. 查看P-003卡片第二行。
- 预期结果
  1. P-001第二行展示电池图标、85%、"最近更新于2026-09-22 10:30"，图标绿色。
  2. P-003离线仍展示电池图标、60%与最近更新时间，在线离线均适用电量展示。

## [P4] 验证电量颜色边界19与20
- 测试类型
  - 功能
- 前置条件
  1. P-005电量19%，P-006电量20%，均为有效电量。
- 测试步骤
  1. 查看P-005卡片电池图标颜色。
  2. 查看P-006卡片电池图标颜色。
- 预期结果
  1. P-005电量19%<20%，电池图标红色。
  2. P-006电量20%≥20%，电池图标绿色，阈值边界判定正确。

## [P3][反向] 验证无有效电量时隐藏电池图标与数值
- 测试类型
  - 功能
- 前置条件
  1. P-007接口未返回有效电量（缺失或越界如150%）。
- 测试步骤
  1. 查看P-007卡片第二行。
  2. 对比相邻有电量设备卡片的第二行布局高度。
- 预期结果
  1. P-007卡片隐藏电池图标和电量数值，不展示技术状态占位文案，不显示0%。
  2. 第二行布局高度保持不变，仅展示"最近更新于"时间，越界电量不按0%触发低电红色。

## [P2] 验证在线设备第三行展示定位频率预设文案
- 测试类型
  - 功能
- 前置条件
  1. P-001最后确认频率1分钟，P-002为10分钟，P-003为60分钟，均在线。
- 测试步骤
  1. 查看P-001卡片第三行。
  2. 查看P-002卡片第三行。
  3. 查看P-003卡片第三行。
- 预期结果
  1. P-001展示"1分钟定位（耗电模式）"。
  2. P-002展示"10分钟定位（默认模式）"。
  3. P-003展示"1小时定位（省电模式）"，三档预设文案与分钟值映射正确。

## [P2] 验证自定义频率展示自定义模式文案
- 测试类型
  - 功能
- 前置条件
  1. P-004在线，最后确认频率为自定义30分钟。
  2. P-008在线，最后确认频率为自定义1440分钟。
- 测试步骤
  1. 查看P-004卡片第三行。
  2. 查看P-008卡片第三行。
- 预期结果
  1. P-004展示"自定义模式（30分钟）"。
  2. P-008展示"自定义模式（1440分钟）"，1440上边界正常展示。

## [P3][反向] 验证无最后确认值时卡片隐藏定位频率文本
- 测试类型
  - 功能
- 前置条件
  1. P-009在线，无任何后端确认成功的历史UPLOAD记录，也无设置中目标。
- 测试步骤
  1. 查看P-009卡片第三行。
- 预期结果
  1. 卡片隐藏定位频率文本，不展示"10分钟定位（默认模式）"等任何占位或默认文案。

## [P1] 验证离线设备第三行统一展示离线提示
- 测试类型
  - 功能
- 前置条件
  1. P-003离线且有已知频率60分钟，P-004初始化未连接。
- 测试步骤
  1. 查看P-003卡片第三行。
  2. 查看P-004卡片第三行。
- 预期结果
  1. P-003第三行展示"离线，无法下发指令"，不展示已知的定位频率文案。
  2. P-004同样展示"离线，无法下发指令"，离线与未连接统一替换为离线提示。

## [P2] 验证告警角标保留在线离线差异
- 测试类型
  - 功能
- 前置条件
  1. P-005告警且在线，P-006告警且离线。
- 测试步骤
  1. 查看P-005卡片左侧图标角标。
  2. 查看P-006卡片左侧图标角标。
- 预期结果
  1. P-005图标红色告警并带在线角标。
  2. P-006图标红色告警并带离线角标，告警态仍保留底层通信状态差异。

## [P2] 验证长备注单行尾部省略不挤压更多入口
- 测试类型
  - 易用性
- 前置条件
  1. P-010备注为40字符长文本"这是一个用于验证卡片布局边界的超长设备备注名称AAAA"。
- 测试步骤
  1. 查看P-010卡片第一行展示与右上角更多入口。
  2. 查看左侧设备图标位置。
- 预期结果
  1. 第一行超长尾部省略号截断，不换行挤压更多入口，更多入口可正常点击。
  2. 设备图标与角标固定尺寸和位置，不被长文本挤压或遮挡。

## [P2] 验证卡片本体点击各状态均进入设备详情
- 测试类型
  - 功能
- 前置条件
  1. P-001在线、P-003离线、P-004未连接、P-005告警，四台设备均可见。
- 测试步骤
  1. 依次点击P-001、P-003、P-004卡片本体。
  2. 点击P-005告警卡片本体。
- 预期结果
  1. 在线、离线、未连接三种状态均跳转到对应设备详情页，离线与未连接卡片不呈现为不可点击整卡置灰。
  2. 告警状态同样跳转到P-005详情页，跳转均携带设备唯一标识。


## POINT-004 单设备更多操作菜单跳转

## [P1] 验证更多操作菜单七项跳转目标正确
- 测试类型
  - 功能
- 前置条件
  1. alice01名下P-001可见且在线。
- 测试步骤
  1. 点击P-001卡片右上角竖向三点打开操作面板。
  2. 依次点击Device Information、Real-Time Tracking、Track History、Device Alert History、IoT SIM Query、Send Command并逐一从列表重新进入验证。
- 预期结果
  1. 操作面板展示设备详情、实时追踪、历史轨迹、设备告警记录、物联网卡查询、发送指令、解除当前账户绑定七个菜单项。
  2. 每个菜单项分别进入当前设备对应的详情页、实时追踪页、历史轨迹页、告警记录页、物联网卡查询页、指令详情页，跳转目标与菜单语义一致。

## [P1] 验证所有跳转携带设备唯一标识
- 测试类型
  - 功能
- 前置条件
  1. P-001与P-002名称均为"牛群一号"，备注不同。
- 测试步骤
  1. 从P-001卡片更多菜单进入Device Information。
  2. 返回列表，从P-002卡片更多菜单进入Device Information。
  3. 对比两个详情页的Device ID。
- 预期结果
  1. P-001详情页Device ID为P-001的唯一标识。
  2. P-002详情页Device ID为P-002的唯一标识。
  3. 名称相同的两台设备不互相串页，身份以唯一标识为准，不以名称、备注或列表位置查询。

## [P2] 验证跳转前后设备名称变化仍指向原设备
- 测试类型
  - 功能
- 前置条件
  1. P-001当前备注为"改名前备注"。
- 测试步骤
  1. 在另一端将P-001备注修改为"改名后备注"。
  2. APP不刷新列表，直接从P-001卡片（仍显示旧名）更多菜单进入Real-Time Tracking。
- 预期结果
  1. 实时追踪页聚焦的仍是P-001唯一标识对应的设备。
  2. 页面设备身份不因名称在跳转前后变化而指向错误设备。

## [P2] 验证更多入口与卡片本体点击相互独立
- 测试类型
  - 易用性
- 前置条件
  1. 设备列表展示P-001。
- 测试步骤
  1. 点击P-001卡片右上角竖向三点。
  2. 观察是否触发设备详情跳转。
- 预期结果
  1. 仅打开当前设备的More操作面板。
  2. 不先触发设备详情跳转，两个点击区域相互独立。

## [P3][反向] 验证菜单项点击后只执行一次目标跳转
- 测试类型
  - 功能
- 前置条件
  1. 先从P-002更多菜单进入过一次IoT SIM Query后返回列表。
- 测试步骤
  1. 从P-001更多菜单点击IoT SIM Query。
  2. 快速连续点击该菜单项2次。
  3. 查看最终打开的页面与查询对象。
- 预期结果
  1. 操作面板关闭并执行一次跳转，进入P-001的物联网卡查询页。
  2. 不叠加打开两个页面，不重复入栈。
  3. 查询对象为P-001，不串到上一次操作的P-002。

## [P2] 验证离线与告警状态设备更多入口保留
- 测试类型
  - 功能
- 前置条件
  1. P-003离线、P-004未连接、P-005告警，均可见。
- 测试步骤
  1. 查看P-003、P-004、P-005卡片右上角更多入口。
  2. 点击P-003更多菜单任一项（如Device Information）。
- 预期结果
  1. 三种状态卡片均保留更多入口，不被长文本遮挡。
  2. 离线设备菜单跳转正常执行，不因通信状态隐藏入口。

## [P2] 验证从目标页返回不改变设备归属与状态
- 测试类型
  - 功能
- 前置条件
  1. P-001归属"天河区"分组，在线，无告警。
- 测试步骤
  1. 从P-001更多菜单依次进入实时追踪、历史轨迹、物联网卡查询后返回设备列表。
  2. 查看P-001的分组归属、在线状态、告警状态。
- 预期结果
  1. 沿设备模块已有导航状态正常返回列表。
  2. P-001仍归属"天河区"、仍在线、告警状态不变，查看扩展项不产生设备关系或状态副作用。


## POINT-005 设备告警记录入口与数据范围

## [P1] 验证从设备更多菜单进入只查询该设备告警
- 测试类型
  - 功能
- 前置条件
  1. P-001有3条历史告警，P-002有2条历史告警，均属alice01可见范围。
- 测试步骤
  1. 从P-001卡片更多菜单点击Device Alert History。
  2. 查看告警记录列表内容。
- 预期结果
  1. 携带P-001唯一标识进入Device Alert页面。
  2. 列表仅出现P-001的3条告警，不混入P-002的任何记录。

## [P2] 验证设备告警与告警中心同源数据
- 测试类型
  - 功能
- 前置条件
  1. P-001存在1条未处理告警AL-20260922-01。
- 测试步骤
  1. 进入告警中心查看AL-20260922-01的描述与时间。
  2. 从P-001更多菜单进入Device Alert History查看同一条记录。
  3. 对比两处记录内容。
- 预期结果
  1. 告警中心正常展示该记录。
  2. 设备入口也能查询到AL-20260922-01。
  3. 两处描述、时间、状态一致，数据同源，仅设备入口固定增加当前设备范围。

## [P2] 验证无告警记录展示设备专属空态
- 测试类型
  - 功能
- 前置条件
  1. P-004从未产生任何告警记录。
- 测试步骤
  1. 从P-004更多菜单进入Device Alert History。
  2. 查看页面展示。
- 预期结果
  1. 查询正常返回。
  2. 展示该设备暂无告警记录的空态，不用其他设备告警填充页面。

## [P3][反向] 验证同名设备各自进入只查各自告警
- 测试类型
  - 功能
- 前置条件
  1. 设备A（P-001）与设备B（P-002）名称均为"牛群一号"，各有1条不同告警。
- 测试步骤
  1. 从设备A更多菜单进入Device Alert History查看记录。
  2. 返回列表，从设备B更多菜单进入查看记录。
- 预期结果
  1. 从A进入只能查询A的告警。
  2. 从B进入只能查询B的告警，不复用上一次查看设备，不按名称匹配。

## [P3][反向] 验证设备关系失效时拦截不展示越权数据
- 测试类型
  - 安全性
- 前置条件
  1. alice01已从P-001更多菜单进入过Device Alert History。
  2. 在另一端解除alice01与P-001的绑定关系。
- 测试步骤
  1. APP内从P-001更多菜单再次进入Device Alert History。
  2. 查看页面返回内容。
- 预期结果
  1. 服务端按最新权限返回失败结果。
  2. 页面展示对应失败结果，不回退查询其他设备告警，不展示越权历史数据。


## POINT-006 物联网卡查询入口与链路

## [P1] 验证首次进入自动查询物联网卡信息
- 测试类型
  - 功能
- 前置条件
  1. P-001已关联物联卡，MSISDN为8601234567890。
- 测试步骤
  1. 从P-001卡片更多菜单点击IoT SIM Query。
  2. 观察页面加载行为与展示内容。
- 预期结果
  1. 进入页面后自动发起当前设备物联卡查询，APP仅提交P-001设备唯一标识。
  2. 查询成功后展示设备摘要、激活状态与SIM明细，前端不选择、输入或拼接第三方卡号。

## [P2] 验证刷新按钮重新查询同一设备
- 测试类型
  - 功能
- 前置条件
  1. 已在P-001的IoT SIM Query页面。
- 测试步骤
  1. 点击右上角刷新按钮。
  2. 查看查询对象与页面数据。
- 预期结果
  1. 重新对P-001发起查询，查询对象不变。
  2. 页面按最新返回刷新字段值。

## [P2] 验证连续查询不同设备数据全部切换
- 测试类型
  - 功能
- 前置条件
  1. P-001关联卡ICCID为8986001234567890123，P-002关联卡ICCID为8986009876543210987。
- 测试步骤
  1. 从P-001更多菜单进入IoT SIM Query记录页面数据。
  2. 返回设备列表，从P-002更多菜单进入IoT SIM Query。
  3. 对比标题设备摘要、ICCID、卡状态和流量数据。
- 预期结果
  1. 第一次查询展示P-001及其ICCID 8986001234567890123。
  2. 第二次进入后标题设备摘要、ICCID、状态、流量全部切换为P-002的数据。
  3. 页面无P-001残留数据，不串卡。

## [P3][反向] 验证无关联卡号进入统一查询失败态
- 测试类型
  - 功能
- 前置条件
  1. P-003未关联任何可用MSISDN、ICCID或IMSI。
- 测试步骤
  1. 从P-003更多菜单进入IoT SIM Query。
  2. 查看页面展示。
- 预期结果
  1. 仍进入查询链路并最终返回统一失败结果。
  2. 设备摘要保留，SIM明细各字段显示"—"，展示可读失败原因与刷新入口，不固定映射为第三方502。

## [P3][反向] 验证第三方超时进入统一失败态并可重试
- 测试类型
  - 可靠性
- 前置条件
  1. 使用模拟工具将第三方单卡接口响应延迟设为超过超时阈值。
- 测试步骤
  1. 从P-001更多菜单进入IoT SIM Query。
  2. 查看超时后的页面展示。
  3. 恢复第三方接口后点击刷新按钮。
- 预期结果
  1. 超时后进入统一查询失败态，不无限加载。
  2. 设备摘要保留，SIM明细显示"—"并展示可读原因。
  3. 刷新后重新查询成功并恢复正常字段展示。

## [P3][反向] 验证查询中设备失权按最新权限拦截
- 测试类型
  - 安全性
- 前置条件
  1. alice01已进入P-001的IoT SIM Query页面。
  2. 另一端解除alice01与P-001绑定。
- 测试步骤
  1. 在失权后的页面上点击刷新。
  2. 再次从P-001更多菜单（若列表仍在）尝试进入IoT SIM Query。
- 预期结果
  1. 服务端按最新权限返回失败。
  2. 不展示旧关联卡或其他账户的受保护卡数据。

## [P2] 验证连续点击刷新仍针对当前设备
- 测试类型
  - 功能
- 前置条件
  1. 已在P-001的IoT SIM Query页面，网络正常。
- 测试步骤
  1. 快速连续点击刷新按钮3次。
  2. 查看每次请求的查询对象与最终页面。
- 预期结果
  1. 每次请求均携带P-001唯一标识，不切换查询对象。
  2. 页面提供基础加载反馈，最终展示P-001最新一次查询结果，无新增倒计时或冷却交互。


## POINT-007 物联网卡页面字段映射

## [P1] 验证查询成功时全字段正常展示与单位换算
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回code=200，active=true，iccid=8986001234567890123，carrier=中国移动，account_status=00，data_plan=2048（MB），当月已用data_usage=512（MB），data_balance=1536（MB），expiry_date=2027-06-30。
- 测试步骤
  1. 从P-001更多菜单进入IoT SIM Query。
  2. 逐项核对页面字段。
- 预期结果
  1. 顶部展示Active；ICCID原值8986001234567890123；运营商"中国移动"；卡状态"正使用"。
  2. 流量套餐、已用流量、剩余流量按接口值等价换算展示（如512MB可展示为0.5GB，数值等价），有效期展示2027-06-30，各字段与返回一致。

## [P2] 验证active为false展示Inactive
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回code=200，active=false，其余字段有值。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看顶部激活标识与明细字段。
- 预期结果
  1. 顶部展示Inactive，false作为有效布尔值展示。
  2. 不因布尔值为false将active显示为"—"，其余字段正常展示。

## [P3][反向] 验证单字段为空保留标签显示占位符
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回code=200，但carrier为null、data_balance为空字符串，其余字段有值。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看运营商与剩余流量两行及其余字段。
- 预期结果
  1. 运营商、剩余流量保留字段标签，值显示"—"。
  2. 其余有值字段正常展示，部分字段为空不影响整页其他字段。

## [P2] 验证卡状态码表映射正确
- 测试类型
  - 功能
- 前置条件
  1. 依次将第三方 mock 的account_status置为10、02、03、04、11、12、15。
- 测试步骤
  1. 每次修改后刷新P-001的IoT SIM Query页面。
  2. 逐个核对卡状态展示。
- 预期结果
  1. 依次展示：测试期、停机、预销号、销号、沉默期、停机保号、库存期。
  2. 每个码值按码表翻译展示，不显示原始码给普通用户造成误解。

## [P3][反向] 验证未知状态码展示未知不崩溃
- 测试类型
  - 可靠性
- 前置条件
  1. 第三方 mock 返回account_status=99，其余字段正常。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看卡状态展示与页面整体状态。
- 预期结果
  1. 卡状态展示"未知"，可保留原码用于排查。
  2. 页面不空白、不崩溃，其余字段正常展示。

## [P2] 验证流量值为0按有效值展示
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回当月已用流量为0，剩余流量为0.000。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看已用流量与剩余流量展示。
- 预期结果
  1. 已用流量展示0及对应单位。
  2. 剩余流量展示0，不把数值0当作无值显示"—"。

## [P2] 验证ICCID前导零与长数字完整展示
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回iccid=008986001234567890123（前导零20位）。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 核对ICCID展示内容与位数。
- 预期结果
  1. ICCID按字符串完整展示008986001234567890123。
  2. 前导零不丢失、不转数值、不出现科学计数法。

## [P3][反向] 验证接口状态码不当作卡状态展示
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回code=502（物联卡不存在或未启用）。
  2. 另一次 mock 返回code=505（访问频率限制）。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面查看502场景。
  2. 切换 mock 为505后刷新页面。
- 预期结果
  1. 502场景展示"物联卡不存在或未启用"可读原因，SIM明细为"—"，卡状态不展示502。
  2. 505场景展示访问频率受限的可读原因并允许稍后刷新，不无限加载，接口code不进入卡状态字段。

## [P2] 验证页面固定元素不受接口影响
- 测试类型
  - 功能
- 前置条件
  1. P-001设备名称为"牧场一号牛"，型号为TL-100。
- 测试步骤
  1. 查询成功场景进入IoT SIM Query页面。
  2. 查询失败场景（第三方断开）再次进入。
  3. 两种场景下核对标题、返回按钮、刷新按钮、设备摘要。
- 预期结果
  1. 标题固定展示"IoT SIM Query"，返回与刷新按钮固定展示。
  2. 设备名称展示"牧场一号牛"、类型展示TL-100，查询失败后摘要仍保留。
  3. 查询失败后刷新按钮仍可重试。


## POINT-008 物联网卡边界与有效期

## [P4] 验证有效期当天仍有效
- 测试类型
  - 功能
- 前置条件
  1. 系统时间2026-09-22，第三方 mock 返回expiry_date=2026-09-22。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看有效期展示。
- 预期结果
  1. 有效期展示2026-09-22。
  2. 当天不显示为已失效，不提前一天按过期处理。

## [P4] 验证有效期已过期按真实日期展示
- 测试类型
  - 功能
- 前置条件
  1. 系统时间2026-09-22，第三方 mock 返回expiry_date=2026-09-20。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看有效期展示。
- 预期结果
  1. 有效期按真实日期展示2026-09-20。
  2. 不虚构过期样式作为通过标准（是否增加过期样式以产品设计定稿为准，记录实际表现）。

## [P3][反向] 验证共享累计卡空流量不伪造为0
- 测试类型
  - 功能
- 前置条件
  1. P-001关联共享累计卡，第三方 mock 返回data_balance为null。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看剩余流量展示。
- 预期结果
  1. 剩余流量显示"—"。
  2. 不把空值写成0，流量池数据不由APP猜测（如需由服务端另行查询）。

## [P4] 验证卡状态字符串00不误判为空
- 测试类型
  - 功能
- 前置条件
  1. 第三方 mock 返回account_status为字符串"00"。
- 测试步骤
  1. 进入P-001的IoT SIM Query页面。
  2. 查看卡状态与激活标识。
- 预期结果
  1. 卡状态展示"正使用"。
  2. 字符串"00"不转为数值0后被误判为空或未激活，也不显示"—"。

## [P5] 验证访问频率限制场景可稍后刷新
- 测试类型
  - 功能
- 前置条件
  1. 使用脚本对同一卡连续发起超过160次/分钟的单卡查询触发第三方505。
- 测试步骤
  1. 触发505后进入P-001的IoT SIM Query页面。
  2. 查看页面展示。
  3. 等待1分钟后点击刷新。
- 预期结果
  1. 展示访问频率受限的可读原因。
  2. 页面可正常交互，不无限加载。
  3. 稍后刷新恢复查询成功（本节不验证新增倒计时或冷却交互）。

## [P3][反向] 验证非JSON与缺data响应进入统一失败态
- 测试类型
  - 可靠性
- 前置条件
  1. 使用 mock 使第三方分别返回HTML错误页（非JSON）与缺少data字段的JSON。
- 测试步骤
  1. 非JSON场景进入P-001的IoT SIM Query页面。
  2. 切换为缺data场景后刷新页面。
- 预期结果
  1. 非JSON响应进入统一查询失败态，设备摘要保留、明细"—"、可读原因与刷新入口齐备。
  2. 缺data响应同样进入统一失败态，两种异常均不保留本次未确认的伪成功数据。

# US-016 绑定牛马定位器


## POINT-001 绑定入口与账号类别分流

## [P1] 验证个人账号展示绑定设备入口并绑定成功
- 测试类型
  - 功能
- 前置条件
  1. 个人账号alice01已登录APP，名下无设备。
  2. 设备P-100已在管理后台入库，类型为牛马定位器。
  3. alice01有剩余绑定配额，名下已有分组"我的分组"。
- 测试步骤
  1. 进入设备管理，查看「绑定设备」入口。
  2. 点击绑定设备，手动输入P-100的设备卡号。
  3. 通过全部校验后在确认页选择"我的分组"，点击确认。
- 预期结果
  1. 设备管理页展示「绑定设备」入口。
  2. 校验通过进入绑定确认页。
  3. 绑定关系落到alice01名下，设备列表出现P-100，各状态数量+1。

## [P1] 验证企业一级账号展示绑定设备入口并绑定成功
- 测试类型
  - 功能
- 前置条件
  1. 企业一级账号owner01已登录APP，有剩余一级配额，名下有分组"我的分组"。
  2. P-101已入库，类型牛马定位器，未被任何企业一级绑定。
- 测试步骤
  1. owner01进入设备管理，点击「绑定设备」。
  2. 扫描P-101机身二维码。
  3. 确认页保持默认名称与分组，点击确认。
- 预期结果
  1. 绑定入口展示，扫码取得P-101设备卡号。
  2. 绑定关系落到owner01（企业一级）名下。
  3. APP与监控平台Web的owner01设备列表同步可见P-101。

## [P3][反向] 验证企业二级账号隐藏绑定入口
- 测试类型
  - 功能
- 前置条件
  1. 企业二级账号worker02已登录APP，已获父级owner01分配的设备P-101。
- 测试步骤
  1. worker02进入设备管理页。
  2. 查找「绑定设备」入口与设备空状态文案。
- 预期结果
  1. 设备管理页不展示「绑定设备」入口。
  2. 正常展示父级已分配的P-101；若无设备时展示普通设备空状态，不展示"联系上级分配"等权限说明。

## [P3][反向] 验证企业三级账号隐藏绑定入口
- 测试类型
  - 功能
- 前置条件
  1. 企业三级账号worker03已登录APP，已获父级worker02分配的设备P-101。
- 测试步骤
  1. worker03进入设备管理页。
  2. 查找「绑定设备」入口。
- 预期结果
  1. 不展示「绑定设备」入口。
  2. 只读取直属二级分配的设备，不进入绑定流程。

## [P3][反向] 验证个人账号不能作为后台代绑目标
- 测试类型
  - 功能
- 前置条件
  1. 管理后台运营账号可执行代绑操作。
  2. 个人账号alice01存在。
- 测试步骤
  1. 在管理后台发起代绑，尝试选择alice01作为代绑目标。
  2. 换用企业一级账号owner01作为代绑目标执行代绑P-102。
- 预期结果
  1. 后台不提供个人账号作为代绑目标选项。
  2. 代绑owner01成功，绑定关系落owner01名下不落操作员名下，owner01在APP与Web同步可见P-102。

## [P2] 验证APP唯一常规绑定入口为设备管理绑定设备
- 测试类型
  - 易用性
- 前置条件
  1. alice01已登录，名下无设备。
- 测试步骤
  1. 依次检查首页、无设备空状态页、全局扫码入口是否存在绑定快捷入口。
  2. 从设备管理 → 绑定设备发起绑定。
- 预期结果
  1. 首页、空状态页、全局位置均无额外绑定快捷入口。
  2. 唯一常规入口为设备管理 → 绑定设备。

## [P2] 验证Web绑定与APP绑定使用同一绑定关系
- 测试类型
  - 兼容性
- 前置条件
  1. alice01同时可用APP与监控平台Web。
  2. P-103已入库为牛马定位器。
- 测试步骤
  1. alice01在监控平台Web绑定P-103。
  2. APP下拉刷新设备列表。
  3. alice01在APP解绑P-103后，登录Web查看。
- 预期结果
  1. Web绑定成功。
  2. APP同步可见P-103，任一端绑定成功后两端同步可见。
  3. Web同样不可见P-103，两端使用同一绑定关系，无独立副本。

## [P3][反向] 验证APP不提供企业向下分配入口
- 测试类型
  - 功能
- 前置条件
  1. owner01已登录APP，名下已绑定P-101。
- 测试步骤
  1. 在APP设备管理、设备详情、更多菜单中查找"分配设备给下级"类入口。
  2. owner01在监控平台Web将P-101分配给worker02。
  3. worker02登录APP查看。
- 预期结果
  1. APP无任何向下分配入口。
  2. Web分配操作成功。
  3. worker02的APP列表出现P-101，APP只读取分配结果。


## POINT-002 绑定校验顺序与拦截清单

## [P1] 验证已入库牛马定位器通过全部校验绑定成功
- 测试类型
  - 功能
- 前置条件
  1. alice01已登录，P-100已入库且类型牛马定位器，配额充足，槽位空闲。
- 测试步骤
  1. 发起绑定输入P-100卡号。
  2. 通过全部校验，确认页选择"我的分组"后确认。
  3. 查看设备列表与状态数量。
- 预期结果
  1. 各项校验通过，进入绑定确认页。
  2. 绑定成功，列表出现P-100。
  3. 设备总数与状态计数刷新，分组直属设备数+1。

## [P3][反向] 验证扫码或输入无法取得有效卡号被拦截
- 测试类型
  - 功能
- 前置条件
  1. 准备一张内容为非设备编码的二维码（如URL https://example.com）。
- 测试步骤
  1. 点击绑定设备选择扫码，扫描上述二维码。
  2. 手动输入"###"等无效字符提交。
- 预期结果
  1. 提示输入/扫码结果无效。
  2. 不进入后续查库与确认页，停留输入阶段。

## [P3][反向] 验证未入库设备绑定被拦截
- 测试类型
  - 功能
- 前置条件
  1. 卡号P-999未在管理后台入库。
  2. alice01绑定配额同时已满（剩余0）。
- 测试步骤
  1. alice01发起绑定输入P-999。
  2. 查看提示内容。
- 预期结果
  1. 绑定被拒绝。
  2. 提示设备不存在/未入库；同时命中未入库与配额满时，优先提示未入库，不以配额不足作为本次结果（校验顺序第2优先于第5）。

## [P3][反向] 验证不支持设备类型绑定被拦截
- 测试类型
  - 功能
- 前置条件
  1. 设备P-200已入库，类型为北斗终端（非牛马定位器）。
- 测试步骤
  1. alice01发起绑定输入P-200卡号。
  2. 查看提示内容。
- 预期结果
  1. 绑定被拒绝。
  2. 提示"当前设备类型暂不支持"，不误提示为设备不存在（类型以后台入库维护的类型字段为准，不以名称或二维码文案判断）。

## [P3][反向] 验证账号已绑定该设备提示已绑定不重复新增
- 测试类型
  - 功能
- 前置条件
  1. alice01已绑定P-100。
- 测试步骤
  1. alice01再次发起绑定输入P-100。
  2. 查看提示与设备列表。
- 预期结果
  1. 提示"已绑定"，不新增关系。
  2. 设备列表保持一条记录，数量不增加，不重复占用配额，可直接打开该设备详情。

## [P3][反向] 验证账号配额不足绑定被拦截
- 测试类型
  - 功能
- 前置条件
  1. alice01当前配额已用满（服务端配置上限N台，已绑N台）。
  2. P-104已入库牛马定位器，槽位空闲。
- 测试步骤
  1. alice01发起绑定输入P-104。
  2. 查看提示内容。
- 预期结果
  1. 绑定被拒绝。
  2. 提示数量已达上限/可绑定设备数量不足，不进入确认页。

## [P3][反向] 验证已被其他个人账号绑定被拦截
- 测试类型
  - 功能
- 前置条件
  1. 个人账号bob02已绑定P-105。
  2. alice01发起绑定。
- 测试步骤
  1. alice01输入P-105卡号发起绑定。
  2. 查看提示内容。
- 预期结果
  1. 绑定被拒绝。
  2. 提示已被其他账号绑定（个人之间互斥），P-105在alice01列表不出现。

## [P3][反向] 验证已被其他企业一级账号绑定被拦截
- 测试类型
  - 功能
- 前置条件
  1. 企业一级账号corpA01已绑定P-106。
  2. 企业一级账号owner01发起绑定。
- 测试步骤
  1. owner01输入P-106卡号发起绑定。
  2. 查看提示内容。
- 预期结果
  1. 绑定被拒绝。
  2. 提示已被其他企业账号绑定（企业一级之间互斥）。

## [P3][反向] 验证无有效分组时绑定引导创建一级组
- 测试类型
  - 功能
- 前置条件
  1. 新账号alice02登录APP，名下删除了初始"我的分组"后处于零业务分组状态。
  2. P-107已入库牛马定位器，其余绑定条件满足。
- 测试步骤
  1. alice02发起绑定P-107。
  2. 在引导进入的分组管理页点击「＋」创建一级组"牧场东区"。
  3. 创建成功后观察绑定流程。
- 预期结果
  1. 绑定流程引导进入分组管理页创建一级组。
  2. 一级组"牧场东区"创建成功。
  3. 返回绑定流程并自动选中"牧场东区"，继续完成绑定。

## [P2] 验证设备未激活无SIM离线仍可绑定
- 测试类型
  - 功能
- 前置条件
  1. P-108已入库牛马定位器，未激活、无SIM、从未上报数据。
  2. 其余绑定条件满足。
- 测试步骤
  1. alice01发起绑定输入P-108。
  2. 完成确认页并提交。
- 预期结果
  1. 激活状态、物联网卡状态、在线与上报状态均不作为绑定前置，不拦截。
  2. 绑定成功，P-108以未连接归入离线展示。


## POINT-003 绑定槽位双绑与配额

## [P1] 验证个人与企业一级双绑同一设备共存
- 测试类型
  - 功能
- 前置条件
  1. 个人账号alice01已绑定P-110。
  2. 企业一级账号owner01有剩余配额，P-110企业一级槽位空闲。
- 测试步骤
  1. owner01发起绑定P-110。
  2. 分别查看alice01与owner01的设备列表。
- 预期结果
  1. owner01绑定成功，已有个人绑定不单独阻断新的企业一级绑定。
  2. alice01与owner01均可见P-110，两个账户读取同一份位置、电量、运行状态事实。

## [P3][反向] 验证设备已有企业一级绑定时个人绑定不被阻断
- 测试类型
  - 功能
- 前置条件
  1. 企业一级账号owner01已绑定P-111。
  2. 个人账号alice01配额充足。
- 测试步骤
  1. alice01发起绑定P-111。
  2. 查看绑定结果。
- 预期结果
  1. alice01绑定成功，不以已有企业一级绑定为拒绝理由。
  2. 个人+企业一级各1个绑定共存，双绑成立。

## [P1] 验证同一设备一级分配多个二级不占绑定名额
- 测试类型
  - 功能
- 前置条件
  1. owner01绑定P-112后，已在Web将P-112分配给worker02、worker03两个二级。
- 测试步骤
  1. 分别登录worker02、worker03的APP查看设备列表。
  2. 查看P-112的绑定关系与个人/企业槽位占用。
- 预期结果
  1. 两个二级账号均可见P-112，各账号列表按设备唯一标识去重。
  2. worker02、worker03获得的是分配关系，不占用"1个个人+1个企业一级"直接绑定槽位，不产生直接绑定记录。

## [P2] 验证企业一级绑定配额随服务端配置
- 测试类型
  - 配置
- 前置条件
  1. 服务端当前配置owner01可绑定设备上限为5台，已绑定4台。
  2. P-113已入库牛马定位器且槽位空闲。
- 测试步骤
  1. owner01绑定P-113（第5台）。
  2. 再绑定P-114（第6台）。
  3. 管理端将上限调整为10台后再次绑定P-114。
- 预期结果
  1. 第5台绑定成功。
  2. 第6台被拦截，提示数量已达上限。
  3. 上限调整后P-114绑定成功，APP不写死配额上限，读取服务端当前配置。

## [P2] 验证重复请求不产生多条绑定关系
- 测试类型
  - 可靠性
- 前置条件
  1. alice01配额充足，P-115已入库牛马定位器。
- 测试步骤
  1. alice01在绑定确认页快速连续点击确认按钮3次。
  2. 弱网环境下重试同一绑定请求。
  3. 查看设备列表与绑定关系。
- 预期结果
  1. 网络重试、重复点击或重复请求均只产生一条绑定关系。
  2. 设备列表P-115仅一条记录。
  3. 配额只扣减一次，无重复副作用。

## [P5] 验证解绑后重新绑定分组围栏按原规则重建
- 测试类型
  - 功能
- 前置条件
  1. alice01曾绑定P-116并配置围栏，已解绑（原分组与围栏关联被清除）。
- 测试步骤
  1. alice01重新绑定P-116并选择分组。
  2. 查看原围栏关联与判定基线。
- 预期结果
  1. 重新绑定成功，分组关系重新建立。
  2. 原围栏关联不自动恢复，需重新关联；下一条有效定位只建立基线，不报警。


## POINT-004 绑定确认页与非绑定前置

## [P1] 验证绑定确认页默认名称与分组必选
- 测试类型
  - 功能
- 前置条件
  1. alice01昵称为"牧场主Alice"，名下有分组"我的分组""牧场东区"。
  2. P-117已通过前序校验。
- 测试步骤
  1. 进入P-117绑定确认页。
  2. 查看Device Name默认值与Group默认选中。
  3. 尝试不选分组直接点击确认。
- 预期结果
  1. Device Name默认写入"牧场主Alice"（账号昵称快照），可编辑、允许为空。
  2. Group默认选中排序最靠前的一级分组。
  3. 未选择真实分组时停留确认页并提示补全，不建立绑定关系。

## [P3][反向] 验证虚拟全部分组不能作为归属分组
- 测试类型
  - 功能
- 前置条件
  1. alice01名下有真实分组，绑定确认页分组选择器可打开。
- 测试步骤
  1. 在绑定确认页打开分组选择器。
  2. 尝试选择虚拟"全部分组"作为Group提交。
- 预期结果
  1. 分组选择器中虚拟"全部分组"不可选或提交时被拦截。
  2. 设备groupId不写入虚拟项，归属必须为真实一级/二级/三级分组。

## [P2] 验证确认页不能切换成另一设备
- 测试类型
  - 功能
- 前置条件
  1. alice01已进入P-118的绑定确认页。
- 测试步骤
  1. 在确认页查找更换设备、重新扫码等切换入口。
  2. 直接完成确认。
- 预期结果
  1. 确认页使用前序流程确定的设备唯一标识与卡号，无切换设备入口。
  2. 绑定关系落在P-118，不串设备。

## [P2] 验证定位频率已移出绑定流程
- 测试类型
  - 功能
- 前置条件
  1. alice01进入P-119绑定确认页。
- 测试步骤
  1. 查看确认页字段构成。
  2. 绑定成功后查看设备详情定位频率与指令记录。
- 预期结果
  1. 确认页无Location Interval字段，不填写、暂存或下发定位频率。
  2. 绑定成功后不自动下发频率指令，不创建等待设备上线后执行的配置任务，详情显示"请选择定位频率"。

## [P2] 验证绑定成功后同步范围正确
- 测试类型
  - 功能
- 前置条件
  1. owner01绑定P-120前记录设备总数、状态数量、分组数量、地图可见设备。
- 测试步骤
  1. owner01完成P-120绑定。
  2. 查看APP设备列表、状态数量、分组直属设备数与地图。
  3. 登录worker02查看是否自动获得P-120。
- 预期结果
  1. 列表、设备总数、在线/离线/告警数量、分组直属设备数与地图可见设备全部刷新。
  2. 不自动创建二级或三级分配关系，worker02不可见P-120，需一级在Web分配。
  3. 不自动创建围栏设备关联。

## [P3][反向] 验证设备名称为空时允许绑定
- 测试类型
  - 功能
- 前置条件
  1. alice01进入P-121绑定确认页。
- 测试步骤
  1. 将Device Name清空。
  2. 选择真实分组后点击确认。
- 预期结果
  1. 名称允许为空，不阻断绑定。
  2. 绑定成功后列表第一行直接展示卡号P-121。


## POINT-005 解绑流程与级联边界

## [P1] 验证个人账号解绑仅影响个人侧
- 测试类型
  - 功能
- 前置条件
  1. alice01与owner01双绑P-130。
  2. alice01已将P-130加入个人侧分组与围栏。
- 测试步骤
  1. alice01在APP从P-130更多菜单点击Unbind Device并确认。
  2. 分别查看alice01、owner01的设备列表。
  3. 查看P-130历史轨迹与告警记录。
- 预期结果
  1. alice01在APP与Web均不可见P-130，个人侧分组与围栏设备关联被清除。
  2. owner01的企业链路不受影响，P-130仍可见。
  3. 历史位置、轨迹、告警、指令记录保留。

## [P1] 验证企业一级解绑级联失效全部下级
- 测试类型
  - 功能
- 前置条件
  1. owner01绑定P-131并已分配给worker02（下有三级worker03）、worker04。
- 测试步骤
  1. owner01在APP从P-131更多菜单点击Unbind Device并确认。
  2. 分别登录worker02、worker03、worker04查看设备列表。
  3. 查看个人账号bob02（同设备个人绑定）列表。
- 预期结果
  1. owner01不可见P-131。
  2. worker02、worker03、worker04的P-131关系全部立即失效，整条企业链路分组与围栏关联清除。
  3. bob02的个人绑定关系不受影响，仍可见P-131。

## [P1] 验证企业二级移除仅自身及下三级失效
- 测试类型
  - 功能
- 前置条件
  1. owner01绑定P-132并分配给worker02、worker05。
  2. worker02已将P-132再分配给三级worker06。
- 测试步骤
  1. worker02在APP从P-132更多菜单点击Unbind Device并确认。
  2. 登录worker06查看设备列表。
  3. 登录owner01、worker05查看设备列表。
- 预期结果
  1. worker02不可见P-132。
  2. worker06（该二级下三级）立即失效不可见。
  3. owner01仍可见，worker05及其他分支三级不受影响，级联只向下不向上。

## [P1] 验证企业三级移除仅自身失效
- 测试类型
  - 功能
- 前置条件
  1. worker03已获worker02分配的P-133。
  2. 同二级下另有三级worker07也获分配P-133。
- 测试步骤
  1. worker03在APP从P-133更多菜单点击Unbind Device并确认。
  2. 分别查看owner01、worker02、worker07的设备列表。
- 预期结果
  1. 仅worker03不可见P-133。
  2. 一级、直属二级、同二级下其他三级均不受影响，仅清除该三级的分组与围栏关联。

## [P2] 验证Web用户管理一级回收二级分配
- 测试类型
  - 功能
- 前置条件
  1. owner01绑定P-134并分配给worker02，worker02已再分配给worker08。
- 测试步骤
  1. owner01在Web用户管理移除worker02的P-134。
  2. 登录worker02、worker08查看设备列表。
  3. 登录owner01查看设备列表。
- 预期结果
  1. 移除操作成功提交。
  2. worker02不可见P-134，其下worker08立即失效。
  3. owner01仍可见P-134，其他二级及其三级不受影响，被回收链路分组与围栏关联清除。

## [P2] 验证Web用户管理二级回收三级分配
- 测试类型
  - 功能
- 前置条件
  1. worker02已将P-135分配给worker08、worker09。
- 测试步骤
  1. worker02在Web用户管理移除worker08的P-135。
  2. 登录worker08、worker09查看设备列表。
  3. 登录owner01、worker02查看设备列表。
- 预期结果
  1. 移除操作成功提交。
  2. 仅worker08不可见P-135，worker09不受影响。
  3. 一级与二级仍可见P-135，仅清除worker08的分组与围栏关联。

## [P3][反向] 验证后台改绑等价解绑A加绑定B
- 测试类型
  - 功能
- 前置条件
  1. 企业一级corpA01绑定P-136，已分配给二级subA02（下有三级）。
  2. 企业一级corpB01配额充足。
- 测试步骤
  1. 管理后台执行改绑corpA01 → corpB01。
  2. 分别查看corpA01链路与corpB01链路。
  3. 查看P-136的个人绑定账号bob02。
- 预期结果
  1. corpA01绑定关系删除，其下二/三级分配立即失效，原分组与围栏关联清除。
  2. corpB01获得企业一级绑定，其下二/三级默认不可见，需重新分配。
  3. bob02的个人绑定关系保留，历史数据保留。

## [P2] 验证重新绑定后原分配与围栏不自动恢复
- 测试类型
  - 功能
- 前置条件
  1. owner01曾绑定P-137并分配给worker02、配置围栏后解绑。
- 测试步骤
  1. owner01重新绑定P-137。
  2. 查看worker02设备列表与P-137围栏关联。
- 预期结果
  1. 重新绑定成功。
  2. 原二/三级分配关系不自动恢复，原围栏关联不自动恢复、不迁移，需重新操作。

## [P3][反向] 验证解绑不删除设备本体与历史数据
- 测试类型
  - 功能
- 前置条件
  1. alice01绑定P-138期间产生过轨迹、告警、指令记录。
  2. alice01执行解绑。
- 测试步骤
  1. 解绑后在管理后台查看P-138库存与物联网卡。
  2. alice01重新绑定P-138后查看历史轨迹与告警。
- 预期结果
  1. 设备本体、库存、物联网卡保留，解绑不等于删除设备。
  2. 历史位置、轨迹、告警、指令记录保留；解绑期间无可见关系时不可查看。

# US-017 管理账户内设备分组


## POINT-001 分组入口与数据来源

## [P1] 验证分组管理唯一常规入口路径可达
- 测试类型
  - 功能
- 前置条件
  1. alice01已登录，名下有分组"广东省""浙江省"。
- 测试步骤
  1. 设备首页点击当前分组栏打开Select Group。
  2. 点击底部Manage Groups。
- 预期结果
  1. Select Group打开并展示当前账号分组结构与直属设备数。
  2. 进入Device Groups分组管理页，展示该账号名下一级、二级、三级分组。

## [P2] 验证管理页全量查询含空组与Web创建组
- 测试类型
  - 功能
- 前置条件
  1. alice01名下：一级组"广东省"（空组）、Web创建的二级组"广州市"、三级组"天河区"。
- 测试步骤
  1. 从Manage Groups进入分组管理页。
  2. 查看列表分组构成。
- 预期结果
  1. 管理页重新请求服务端全量分组。
  2. 空组"广东省"、二级"广州市"、三级"天河区"均展示，不从设备列表反向聚合分组，不按创建端过滤。

## [P2] 验证管理页不渲染设备数量
- 测试类型
  - 功能
- 前置条件
  1. alice01名下"广东省"组有直属设备3台。
- 测试步骤
  1. 进入分组管理页查看"广东省"卡片。
  2. 打开分组选择器查看"广东省"条目。
- 预期结果
  1. 管理页卡片展示名称、Level、父组信息、重命名删除入口，无Devices: N数量行。
  2. 选择器中"广东省"括号内展示直属设备数3，数量只在设备首页选择器展示。

## [P3][反向] 验证接口失败不用空数组冒充无分组
- 测试类型
  - 可靠性
- 前置条件
  1. 使用mock使分组查询接口返回失败。
- 测试步骤
  1. 打开Select Group触发分组查询失败。
  2. 查看页面展示。
- 预期结果
  1. 保留原有效页面状态。
  2. 展示可重试的失败提示，不用空数组冒充账号无分组。

## [P2] 验证Web跨端变更后APP读取最新结果
- 测试类型
  - 兼容性
- 前置条件
  1. alice01在APP查看过分组结构。
  2. 随后在Web将二级组"广州市"重命名为"羊城"。
- 测试步骤
  1. APP重新打开Select Group。
  2. 从Select Group进入Manage Groups查看结构。
- 预期结果
  1. 选择器展示最新名称"羊城"。
  2. 管理页再次请求全量分组，不维护独立APP分组副本，跨端以服务端数据为准。

## [P2] 验证从管理页返回保留有效选择
- 测试类型
  - 功能
- 前置条件
  1. 当前设备列表筛选为"天河区"（groupId有效）。
- 测试步骤
  1. 从选择器进入Manage Groups后直接返回。
  2. 查看设备列表筛选与分组栏。
- 预期结果
  1. 返回时重新查询分组数据。
  2. 原选中"天河区"仍有效则保留筛选；列表与状态数量正常。

## [P3][反向] 验证原分组失效回退全部分组
- 测试类型
  - 功能
- 前置条件
  1. 当前设备列表筛选为"旧分组"（groupId=G100）。
  2. 在Web删除该分组。
- 测试步骤
  1. APP从管理页返回（触发重新查询）。
  2. 查看分组栏与设备列表。
- 预期结果
  1. 检测到G100已删除。
  2. 筛选回退虚拟"全部分组"，提示原分组已失效，设备列表与状态数量刷新。

## [P3][反向] 验证APP不能替换前端账号参数读取他账分组
- 测试类型
  - 安全性
- 前置条件
  1. alice01已登录。
  2. 抓包工具可用。
- 测试步骤
  1. 抓取分组查询请求。
  2. 将请求中账号参数改为bob02后重放。
- 预期结果
  1. 服务端按登录会话身份确定账号范围。
  2. 重放请求被拒绝或仍返回alice01范围数据，不返回bob02的分组数据。

## [P2] 验证APP操作成功后同源刷新
- 测试类型
  - 功能
- 前置条件
  1. alice01在分组管理页。
- 测试步骤
  1. APP新增一级组"牧场新区"。
  2. 返回并重新打开Select Group。
- 预期结果
  1. 管理页以服务端返回结果刷新，出现"牧场新区"。
  2. 选择器同步看到最新结构和数量，两页同源。


## POINT-002 分组模型与对象边界

## [P1] 验证三级父子体系分组结构展示
- 测试类型
  - 功能
- 前置条件
  1. alice01名下结构：广东省（一级）→ 广州市（二级）→ 天河区（三级）、深圳市（二级，属广东省）、浙江省（一级）。
- 测试步骤
  1. 打开Select Group查看列表结构。
  2. 查看各分组的Level与父组信息展示。
- 预期结果
  1. 平铺顺序为：广东省L1、广州市L2（属于：广东省）、天河区L3（属于：广州市）、深圳市L2（属于：广东省）、浙江省L1。
  2. 二、三级展示直接父组，一级不展示父组，业务数据保留层级与唯一标识。

## [P1] 验证虚拟全部分组固定置顶不可操作
- 测试类型
  - 功能
- 前置条件
  1. alice01可见设备总数为8台（含多关系去重后）。
- 测试步骤
  1. 打开Select Group查看首项。
  2. 尝试对"全部分组"进行重命名、删除。
- 预期结果
  1. "全部分组（8）"固定置顶，无Level标签，括号为账户全部可见设备去重数。
  2. 虚拟项不可增删改，不进入分组管理页。

## [P2] 验证我的分组为普通一级组可改名可删除
- 测试类型
  - 功能
- 前置条件
  1. alice01账号初始化自动创建"我的分组"（空组）。
- 测试步骤
  1. 在管理页重命名"我的分组"为"默认牧场"。
  2. 将其删除（空组满足删除条件）。
  3. 删除后再次新增名为"我的分组"的一级组。
- 预期结果
  1. 改名成功，分组ID、设备归属、子组关系不变。
  2. 空组删除成功，账户处于零业务分组状态，不自动重建。
  3. "我的分组"名称已释放，允许再次创建。

## [P3][反向] 验证设备必须归属真实分组不存在未分组
- 测试类型
  - 功能
- 前置条件
  1. alice01名下设备P-201当前归属"天河区"。
- 测试步骤
  1. 进入设备详情编辑页尝试清空Group字段保存。
  2. 查看设备列表与分组体系中的"未分组"入口。
- 预期结果
  1. 分组为空时保存被拦截，提示补全。
  2. 系统不存在"未分组"视图或归属状态，绑定和资料保存时分组不能为空。

## [P2] 验证分组按账户隔离
- 测试类型
  - 功能
- 前置条件
  1. alice01与bob02均可见设备P-202（双绑）。
  2. alice01将P-202归"广东省"，bob02归"浙江牧场"。
- 测试步骤
  1. alice01将P-202改组为"浙江省"并保存。
  2. 登录bob02查看P-202分组。
- 预期结果
  1. alice01侧改组成功。
  2. bob02侧P-202仍属"浙江牧场"，同一物理设备在不同账户下分组独立，一方改组不影响另一方。

## [P2] 验证同名真实一级组与虚拟项共存区分
- 测试类型
  - 功能
- 前置条件
  1. alice01创建了名为"全部分组"的真实一级组（直属设备2台）。
- 测试步骤
  1. 打开Select Group查看列表。
  2. 分别点击虚拟"全部分组"与真实"全部分组"。
- 预期结果
  1. 虚拟"全部分组（8）"固定置顶无Level；真实"全部分组（2）"按业务排序展示并标记Level 1。
  2. 虚拟项查询账户全部设备，真实项只查询直属2台，两者可区分不混淆。

## [P2] 验证APP仅能新增一级分组
- 测试类型
  - 功能
- 前置条件
  1. alice01在分组管理页。
- 测试步骤
  1. 点击标题栏「＋」新增分组。
  2. 查看新增弹窗是否提供Level或父组选择。
- 预期结果
  1. 固定创建一级分组。
  2. 不提供Level或父组选择；二、三级分组需由当前账号在Web平台创建。

## [P5] 验证仅分配设备自动复用或创建我的分组
- 测试类型
  - 功能
- 前置条件
  1. 一级owner01执行「仅分配设备」将P-210分配给二级worker02。
  2. worker02名下无名为"我的分组"的真实分组。
- 测试步骤
  1. owner01在Web执行仅分配设备将P-210分配给worker02。
  2. 登录worker02查看分组与设备。
- 预期结果
  1. worker02名下自动创建普通一级"我的分组"，P-210进入该组，原分组保留。
  2. 若目标账号已存在"我的分组"（如worker11）则直接复用，新设备进入该组。

## [P3][反向] 验证同步分组和设备全量覆盖二级分组
- 测试类型
  - 功能
- 前置条件
  1. worker02名下原有分组"旧A组""旧B组"并各有设备。
  2. owner01在Web执行「同步分组和设备」。
- 测试步骤
  1. owner01执行同步分组和设备。
  2. 登录worker02查看分组结构。
- 预期结果
  1. worker02原有分组被全量覆盖。
  2. 分组结构变为owner01同步的结构，属高破坏性操作，执行前需确认业务可接受。


## POINT-003 分组新增与重命名

## [P1] 验证新增一级分组成功
- 测试类型
  - 功能
- 前置条件
  1. alice01在分组管理页，当前无同名"牧场新区"分组。
- 测试步骤
  1. 点击「＋」打开新增弹窗。
  2. 输入"牧场新区"并保存。
- 预期结果
  1. 弹窗展示Group name (1–40 characters)范围提示。
  2. 保存成功，关闭弹窗并刷新分组数据，列表出现一级组"牧场新区"。

## [P4] 验证分组名称长度边界值
- 测试类型
  - 功能
- 前置条件
  1. 海外版APP（本大纲主口径1～40字符）。
  2. 准备1字符"a"、40字符、41字符三组名称。
- 测试步骤
  1. 新增分组输入"a"保存。
  2. 重命名某组输入40字符名称保存。
  3. 新增分组输入41字符名称保存。
- 预期结果
  1. 1字符边界保存成功。
  2. 40字符上点保存成功。
  3. 41字符离点被禁止并提示当前版本长度要求，不静默截断。

## [P5] 验证国内版分组名称长度边界
- 测试类型
  - 兼容性
- 前置条件
  1. 国内版APP（1～15字符）。
  2. 准备15字符与16字符名称。
- 测试步骤
  1. 新增分组输入15字符名称保存。
  2. 输入16字符名称保存。
- 预期结果
  1. 15字符上点保存成功。
  2. 16字符离点被禁止，弹窗展示1–15字符范围提示；上限由产品构建确定，不随界面语言动态变化。

## [P3][反向] 验证空名称与纯空格名称禁止保存
- 测试类型
  - 功能
- 前置条件
  1. alice01在新增/重命名弹窗。
- 测试步骤
  1. 新增分组名称留空直接保存。
  2. 输入4个空格保存。
  3. 重命名时输入纯空格保存。
- 预期结果
  1. 空字符串禁止保存。
  2. 纯空格名称禁止保存。
  3. 两种场景均提示补全，不创建无名分组。

## [P3][反向] 验证同账户跨层级全局判重
- 测试类型
  - 功能
- 前置条件
  1. alice01名下已有：一级"广东省"、二级"广州市"（属广东省）。
- 测试步骤
  1. 新增一级组输入"广州市"保存。
  2. 新增一级组输入"广州市 "（尾部1空格）保存。
  3. 新增一级组输入"广州market"（部分相同）保存。
- 预期结果
  1. 与二级组"广州市"完全同名，全局判重命中，禁止保存并提示分组名称已存在。
  2. 尾部空格参与比较，"广州市 "与"广州市"视为不同名称，保存成功。
  3. 部分相同名称保存成功，判重为完全精确匹配。

## [P2] 验证重命名未变化不误判重名
- 测试类型
  - 功能
- 前置条件
  1. alice01要将一级组"广东省"重命名。
- 测试步骤
  1. 打开重命名弹窗，不修改名称直接保存。
  2. 将名称改为"广东省"（与自身相同）保存。
- 预期结果
  1. 名称未变化时不把当前分组自身误判为重名。
  2. 保存成功（或按服务端实际行为记录），不提示"分组名称已存在"。

## [P2] 验证重命名不影响结构与归属
- 测试类型
  - 功能
- 前置条件
  1. "广东省"（一级）下有二级"广州市"、直属设备P-201。
- 测试步骤
  1. 将"广东省"重命名为"广东"。
  2. 查看分组结构、P-201归属与子组父组名称展示。
- 预期结果
  1. 只更新名称，分组ID、层级、父子关系、设备归属不变。
  2. "广州市"的父组名称展示同步更新为"广东"，存在直属设备或子组仍允许改名。

## [P3][反向] 验证保存失败保留输入并展示原因
- 测试类型
  - 可靠性
- 前置条件
  1. 使用mock使分组保存接口返回失败。
- 测试步骤
  1. 新增分组输入"测试失败组"提交。
  2. 查看弹窗状态。
- 预期结果
  1. 保存失败。
  2. 保留用户输入并展示服务端原因，不白屏不丢输入。


## POINT-004 分组删除规则与交互

## [P1] 验证空组无子组删除成功
- 测试类型
  - 功能
- 前置条件
  1. alice01名下三级组"天河区"无子组、无直属设备。
- 测试步骤
  1. 点击"天河区"删除按钮。
  2. 在二次确认弹窗点击确认。
- 预期结果
  1. 服务端实时校验通过，弹出删除确认并展示分组名称"天河区"。
  2. 删除成功，刷新分组列表，"天河区"消失。

## [P3][反向] 验证存在子组时禁止删除并优先提示
- 测试类型
  - 功能
- 前置条件
  1. 一级组"广东省"下有二级"广州市"（广州市下另有三级"天河区"），且广东省无直属设备。
- 测试步骤
  1. 点击"广东省"删除按钮。
  2. 查看提示与是否进入确认。
- 预期结果
  1. 检测到存在子组（直接或间接）。
  2. 阻止删除并提示先处理子分组；即使同时无直属设备，也只提示子分组阻塞原因，不进入二次确认。

## [P3][反向] 验证存在直属设备时禁止删除
- 测试类型
  - 功能
- 前置条件
  1. 二级组"深圳市"无子组，直属设备P-203、P-204。
- 测试步骤
  1. 点击"深圳市"删除按钮。
  2. 查看提示内容。
- 预期结果
  1. 先校验子组（无），再校验直属设备（有）。
  2. 阻止删除并提示先迁移组内设备，不隐式迁移或级联删除设备。

## [P2] 验证删除确认弹窗取消保留分组
- 测试类型
  - 功能
- 前置条件
  1. 空组"旧牧场"满足删除条件。
- 测试步骤
  1. 点击"旧牧场"删除按钮进入二次确认。
  2. 点击取消。
- 预期结果
  1. 确认弹窗展示分组名称。
  2. 取消后分组保留，列表不变。

## [P2] 验证删除服务端失败保留分组并提示
- 测试类型
  - 可靠性
- 前置条件
  1. 空组"失败测试组"满足前端删除条件。
  2. mock删除接口返回失败。
- 测试步骤
  1. 点击删除并确认。
  2. 查看页面与分组状态。
- 预期结果
  1. 提交删除后服务端返回失败。
  2. 保留分组并展示服务端原因。

## [P2] 验证删除当前筛选组回退全部分组
- 测试类型
  - 功能
- 前置条件
  1. 当前设备列表筛选为"旧分区"（空组）。
- 测试步骤
  1. 在管理页删除"旧分区"并确认。
  2. 返回设备列表查看筛选与数量。
- 预期结果
  1. 删除成功。
  2. 筛选切换到虚拟"全部分组"，设备列表与状态数量刷新；删除非当前筛选组时保留当前有效筛选。

## [P5] 验证删除最后一个真实分组允许零分组状态
- 测试类型
  - 功能
- 前置条件
  1. alice01名下仅剩空组"我的分组"（最后一个真实分组）。
- 测试步骤
  1. 删除"我的分组"并确认。
  2. 查看选择器与管理页。
  3. 发起设备绑定。
- 预期结果
  1. 满足空组条件允许删除，不自动重建。
  2. 账户只剩虚拟"全部分组"，设备不能归属虚拟项。
  3. 绑定时无可选分组，引导进入分组管理页创建一级组。

## [P3][反向] 验证管理页不凭文案判断空组实时校验
- 测试类型
  - 功能
- 前置条件
  1. 分组"表面空组"在管理页不展示设备数量。
  2. 实际该组有直属设备1台（Web侧可见）。
- 测试步骤
  1. 在APP点击"表面空组"删除按钮。
  2. 查看服务端校验结果。
- 预期结果
  1. 点击后向服务端实时校验可删除条件。
  2. 服务端返回有直属设备，阻止删除并提示先迁移，不因页面无数量展示而放行。


## POINT-005 分组选择器展示排序与搜索

## [P1] 验证选择器默认结构与父组不聚合
- 测试类型
  - 功能
- 前置条件
  1. 结构：广东省（直属2台）→广州市（直属3台）→天河区（直属5台）；深圳市（直属1台）；浙江省（直属0台空组）。
- 测试步骤
  1. 打开Select Group查看完整列表构成。
  2. 核对各组括号内数量与账户全部去重数。
- 预期结果
  1. 依次展示：全部分组（账户全部去重数）、广东省（2）L1、广州市（3）L2属于广东省、天河区（5）L3属于广州市、深圳市（1）L2属于广东省、浙江省（0）L1。
  2. 各组括号内仅为直接归属设备数，父组数量可小于子组，空组也展示。

## [P2] 验证一级排序同步Web维护值
- 测试类型
  - 功能
- 前置条件
  1. Web已维护一级组排序：浙江省置前、广东省置后。
- 测试步骤
  1. 打开Select Group查看一级顺序。
  2. 无排序值的另一账号（新建组未设排序）打开选择器。
- 预期结果
  1. 一级顺序按Web维护的排序值展示（浙江省在前）。
  2. 无排序值时按创建时间升序。

## [P2] 验证分组名称搜索包含匹配
- 测试类型
  - 功能
- 前置条件
  1. 名下分组含"广州市""湖北市""广州路"。
- 测试步骤
  1. 在选择器搜索框输入"广州"。
  2. 查看搜索结果。
- 预期结果
  1. 命中"广州市""广州路"。
  2. 结果保留设备数、Level和直接父组信息，仅展示命中项，不自动带出未命中的祖先组，"湖北市"不出现。

## [P3][反向] 验证搜索无结果不改变原筛选
- 测试类型
  - 功能
- 前置条件
  1. 当前筛选为"广东省"。
- 测试步骤
  1. 在选择器搜索"不存在的组名xyz"。
  2. 查看结果与关闭选择器后的列表筛选。
- 预期结果
  1. 展示无匹配分组提示。
  2. 原筛选"广东省"保留不变。

## [P2] 验证选择分组立即应用关闭选择器
- 测试类型
  - 易用性
- 前置条件
  1. 当前筛选为虚拟"全部分组"。
- 测试步骤
  1. 点击目标分组"天河区"。
  2. 观察交互过程。
- 预期结果
  1. 单选，点击后立即应用并关闭选择器返回设备列表，无需额外确认。
  2. 列表刷新为天河区直属设备。

## [P2] 验证未选择关闭保留原筛选
- 测试类型
  - 功能
- 前置条件
  1. 当前筛选为"深圳市"。
- 测试步骤
  1. 打开Select Group浏览分组后直接关闭并返回设备列表。
  2. 查看分组栏当前筛选。
- 预期结果
  1. 未选择任何分组即关闭选择器。
  2. 原筛选"深圳市"保留，列表不刷新。

## [P2] 验证每次打开选择器重新拉取数据
- 测试类型
  - 功能
- 前置条件
  1. 在Web为alice01新增二级组"新城区"。
- 测试步骤
  1. APP打开Select Group。
  2. 查看是否出现"新城区"。
- 预期结果
  1. 打开时重新拉取服务端最新分组数据。
  2. "新城区"出现且数量、层级正确，Web结构变更不被APP缓存覆盖。


## POINT-006 设备筛选范围与状态数量

## [P1] 验证首次进入默认全部分组
- 测试类型
  - 功能
- 前置条件
  1. alice01登录后首次进入设备首页。
- 测试步骤
  1. 进入设备首页查看分组栏选中项。
  2. 查看列表范围与顶部状态数量。
- 预期结果
  1. 默认选择虚拟"全部分组"。
  2. 列表展示账户全部可见设备，状态数量按全部设备统计。

## [P2] 验证同会话保留上次有效分组选择
- 测试类型
  - 功能
- 前置条件
  1. alice01同一登录会话内已选择"天河区"。
- 测试步骤
  1. 切换到首页其他模块后返回设备首页。
  2. 查看分组栏筛选。
- 预期结果
  1. 返回设备首页。
  2. 保留上次有效的"天河区"选择，不重置为全部分组。

## [P3][反向] 验证切换账号不复用上一账号筛选
- 测试类型
  - 安全性
- 前置条件
  1. alice01退出前筛选为"天河区"。
- 测试步骤
  1. alice01退出登录。
  2. bob02登录进入设备首页查看分组栏。
  3. bob02退出，alice01重新登录进入设备首页查看分组栏。
- 预期结果
  1. alice01的会话与筛选状态被清理。
  2. bob02进入后从虚拟"全部分组"开始，不复用alice01的筛选。
  3. alice01重新登录后同样从"全部分组"开始，账号间筛选隔离。

## [P2] 验证父组筛选不聚合子组设备
- 测试类型
  - 功能
- 前置条件
  1. 广东省直属2台（1在线1离线），子组广州市直属3台在线，天河区直属5台告警。
- 测试步骤
  1. 选择"广东省"分组。
  2. 查看列表与顶部状态数量。
- 预期结果
  1. 列表仅展示广东省直属2台。
  2. 全部=2、在线=1、离线=1、告警=0，不聚合广州市、天河区设备。

## [P2] 验证关键词不缩小分组数量与状态数量
- 测试类型
  - 功能
- 前置条件
  1. 当前分组"广东省"（直属2台），状态"在线"。
- 测试步骤
  1. 输入关键词"P-2"并确认。
  2. 查看列表、分组括号数量与顶部状态数量。
- 预期结果
  1. 列表按关键词∩分组∩状态重算。
  2. 分组括号数量与顶部状态数量不因关键词缩小，仅列表结果缩小。

## [P2] 验证切换分组后状态数量重算
- 测试类型
  - 功能
- 前置条件
  1. 全部范围：全部=10、在线=6、离线=4、告警=1。
  2. "天河区"直属5台：4在线1告警（在线）。
- 测试步骤
  1. 从"全部分组"切换到"天河区"。
  2. 查看顶部四个数量。
- 预期结果
  1. 切换生效。
  2. 数量重算为全部=5、在线=5（告警设备同时在线）、离线=0、告警=1，按当前分组范围统计。


## POINT-007 设备改组

## [P1] 验证设备改组随整页保存生效
- 测试类型
  - 功能
- 前置条件
  1. P-201当前归属"天河区"。
  2. alice01名下有目标分组"深圳市"。
- 测试步骤
  1. 进入P-201设备详情编辑页，将Group从"天河区"改为"深圳市"，不点击保存。
  2. 查看两侧分组设备数。
  3. 点击整页Save。
- 预期结果
  1. 选择分组只更新本地表单，不立即改组。
  2. 保存后详情、设备列表、分组设备数与状态数量刷新：深圳市+1、天河区-1。
  3. 改组以groupId提交，不以名称代替。

## [P2] 验证取消编辑不产生变更
- 测试类型
  - 功能
- 前置条件
  1. P-201归属"天河区"，进入编辑页已改选"深圳市"。
- 测试步骤
  1. 不保存直接返回/取消编辑。
  2. 查看P-201分组与两侧数量。
- 预期结果
  1. 本地修改被丢弃。
  2. P-201仍归属"天河区"，无任何分组数量变化。

## [P3][反向] 验证保存前分组失效被拦截
- 测试类型
  - 功能
- 前置条件
  1. 编辑页已选择目标分组"旧目标组"（groupId=G200）。
  2. 保存前该组在Web被删除。
- 测试步骤
  1. 点击整页Save。
  2. 查看保存结果。
- 预期结果
  1. 服务端保存前重新校验分组有效性。
  2. G200已删除，保存失败并提示可读原因，设备仍归属原分组，编辑内容保留。

## [P3][反向] 验证单分组容量超限保存失败
- 测试类型
  - 功能
- 前置条件
  1. 服务端当前配置单分组设备容量上限（如已接近上限）。
- 测试步骤
  1. 将设备改入已满分组并保存。
  2. 查看结果与提示。
- 预期结果
  1. 保存失败。
  2. 展示服务端可读提示，APP不写死具体上限，设备保持原分组。

## [P2] 验证改组只影响当前账户分组关系
- 测试类型
  - 功能
- 前置条件
  1. alice01与owner01双绑P-202。
  2. alice01侧归属"广东省"，owner01侧归属"企业A组"。
- 测试步骤
  1. alice01将P-202改组为"浙江省"并保存。
  2. 登录owner01查看P-202分组。
- 预期结果
  1. alice01侧改组成功。
  2. owner01侧P-202仍属"企业A组"，改组只更新当前账户的设备分组关系。

## [P2] 验证改组成功后列表与搜索同步
- 测试类型
  - 功能
- 前置条件
  1. P-201原备注"旧名牛只"，改组同时不改名称。
- 测试步骤
  1. 完成P-201改组保存。
  2. 按分组"深圳市"筛选查看列表。
  3. 修改P-201备注为"新名牛只"保存后搜索"新名牛只"。
- 预期结果
  1. 改组成功后详情与列表刷新。
  2. "深圳市"分组下出现P-201。
  3. 名称变更后列表展示与搜索结果同步更新，名称展示与搜索同源。

# US-018 编辑设备资料并查询物联网卡


## POINT-001 详情页字段与展示规则

## [P1] 验证详情页字段构成与数据来源
- 测试类型
  - 功能
- 前置条件
  1. P-201备注"牧场一号牛"，联系人"张三"，电话+8613800138000，归属"天河区"，ICCID 8986001234567890123，服务到期2027-06-30，型号TL-100，创建时间2026-01-15。
- 测试步骤
  1. 进入P-201设备详情查看态。
  2. 逐项核对九个字段。
- 预期结果
  1. 展示Device ID、ICCID、Expiration Date（2027-06-30）、Device Model（TL-100）、Group（天河区）、Device Name（牧场一号牛）、Contact（张三）、Phone Number（+8613800138000）、Location Interval、Created At（2026-01-15）。
  2. 各字段值与来源一致，日期按YYYY-MM-DD展示。

## [P2] 验证只读字段不可修改
- 测试类型
  - 功能
- 前置条件
  1. 进入P-201详情编辑态。
- 测试步骤
  1. 尝试修改Device ID、ICCID、Expiration Date、Device Model、Created At。
  2. 查看各字段编辑能力。
- 预期结果
  1. 五个只读字段均不可修改。
  2. Device Model展示实际硬件型号，不由名称或前端选项反推。

## [P3][反向] 验证只读字段缺失统一显示短横线
- 测试类型
  - 功能
- 前置条件
  1. P-203服务端未维护服务到期时间，ICCID无关联卡记录。
- 测试步骤
  1. 进入P-203详情查看态。
  2. 查看Expiration Date与ICCID行展示。
- 预期结果
  1. 两个字段行保留。
  2. 缺失值统一显示"-"，不显示0或伪造日期，客户端不补默认日期。

## [P2] 验证可编辑空字段查看态展示短横线
- 测试类型
  - 功能
- 前置条件
  1. P-204的Device Name、Contact、Phone Number均为空。
- 测试步骤
  1. 查看P-204详情查看态三个字段。
  2. 返回设备列表查看P-204卡片第一行。
- 预期结果
  1. 查看态按UI显示"-"。
  2. 列表标题按"设备名称（备注）优先、Device ID兜底"展示卡号P-204。

## [P1] 验证无确认值时定位频率显示请选择
- 测试类型
  - 功能
- 前置条件
  1. P-205刚绑定，平台无该设备历史成功UPLOAD记录，无设置中目标。
- 测试步骤
  1. 查看P-205详情查看态Location Interval。
  2. 进入编辑态打开频率弹层查看选择框。
- 预期结果
  1. 查看态显示"请选择定位频率"。
  2. 弹层选择框默认显示"请选择定位频率"，不预选目标，不用-、技术状态名或10分钟默认值替代。

## [P2] 验证详情请求失败可恢复重试
- 测试类型
  - 可靠性
- 前置条件
  1. mock设备详情接口返回失败。
- 测试步骤
  1. 进入P-201详情。
  2. 查看页面展示。
  3. 恢复接口后重试进入。
- 预期结果
  1. 保留可恢复页面状态并展示可重试原因。
  2. 不展示残缺或伪造数据。
  3. 重试后正常展示详情。

## [P3][反向] 验证服务端重新校验可见范围
- 测试类型
  - 安全性
- 前置条件
  1. alice01已进入P-206详情页。
  2. 另一端解除alice01与P-206关系。
- 测试步骤
  1. 在详情页执行保存操作。
  2. 抓包构造对P-206的资料保存请求重放。
- 预期结果
  1. 服务端按当前登录账户与Device ID重新校验。
  2. 设备已不可见时保存被拒绝，不依赖前端按钮控制，不产生越权写入。


## POINT-002 可编辑字段校验

## [P1] 验证设备名称编辑与首尾空格处理
- 测试类型
  - 功能
- 前置条件
  1. P-201进入详情编辑态，当前名称"牧场一号牛"。
- 测试步骤
  1. 输入"  牧场二号牛  "（首尾空格）保存。
  2. 输入40字符名称保存。
  3. 输入41字符名称保存。
  4. 输入纯空格保存。
- 预期结果
  1. 保存后名称为"牧场二号牛"，首尾空格去除，内部空格保留。
  2. 40字符保存成功（0～40字符上限内点）。
  3. 41字符保存被拦截。
  4. 纯空格归一为空，等同空值保存成功，列表第一行回退展示卡号。

## [P2] 验证设备名称允许重名
- 测试类型
  - 功能
- 前置条件
  1. P-201名称"同名片"，P-202名称"另一个"。
- 测试步骤
  1. 将P-202名称改为"同名片"保存。
  2. 搜索"同名片"。
- 预期结果
  1. 保存成功，名称允许重名，不执行唯一性校验。
  2. 搜索命中两台设备，设备身份仍以Device ID为准。

## [P2] 验证联系人校验规则
- 测试类型
  - 功能
- 前置条件
  1. P-201编辑态，Contact为空。
- 测试步骤
  1. 输入"  张三  "保存。
  2. 输入40字符联系人（如"José María González-De-La-Cruz-Pérez12345678"）保存。
  3. 输入41字符联系人保存。
  4. 输入纯空格保存。
- 预期结果
  1. 保存为"张三"，首尾空格去除。
  2. 40字符Unicode姓名保存成功。
  3. 41字符被拦截。
  4. 纯空格按空值处理保存成功，联系人不要求唯一。

## [P1] 验证电话号码E.164标准化保存
- 测试类型
  - 功能
- 前置条件
  1. P-201编辑态，国家/地区码选择器可用。
- 测试步骤
  1. 选择中国+86，输入"138 0013 8000"保存。
  2. 选择美国+1，输入"(212) 555-0123"保存。
- 预期结果
  1. 输入层允许空格、短横线、括号格式；保存后标准化为+8613800138000。
  2. 美国号码标准化为+12125550123，规范号码最多15位数字，查看态回显E.164标准值。

## [P2] 验证电话国家码按系统Region预选
- 测试类型
  - 功能
- 前置条件
  1. 测试机系统Region为中国，P-202电话为空。
- 测试步骤
  1. 首次进入P-202编辑态查看国家码选择器。
  2. 对已有号码+8613800138000的P-201进入编辑态。
- 预期结果
  1. 空字段优先按系统Region预选中国+86。
  2. 系统Region不静默改写已保存号码，P-201保持+86原值。

## [P3][反向] 验证无法解析的电话禁止保存
- 测试类型
  - 功能
- 前置条件
  1. P-201编辑态电话字段。
- 测试步骤
  1. 选择国家码+86，输入"abc123!@#"保存。
  2. 不选国家码输入"13800138000"保存。
  3. 输入超过15位数字的号码"13800138000123456"保存。
- 预期结果
  1. 非数字主体无法解析，禁止保存并提示。
  2. 填写本地号码前必须主动选择国家/地区，未选择被拦截。
  3. 超长号码无法规范为E.164，禁止保存。

## [P3][反向] 验证分组必填且实时有效
- 测试类型
  - 功能
- 前置条件
  1. P-201编辑态。
  2. 名下真实分组可用。
- 测试步骤
  1. 尝试清空Group保存。
  2. 选择虚拟"全部分组"作为Group。
- 预期结果
  1. Group必填，清空被拦截。
  2. 虚拟项不能作为设备归属，不写入groupId。


## POINT-003 定位频率弹层与编辑态

## [P1] 验证在线设备频率弹层选择与整页保存
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，无在途目标，最后确认值为10分钟。
- 测试步骤
  1. 进入编辑态点击Location Interval打开弹层。
  2. 查看60分钟选项并选择，点击弹层Save。
  3. 点击整页Save。
- 预期结果
  1. 弹层自动选中"10分钟（默认模式）"预设。
  2. 弹层Save只写入本地表单，不立即发送指令。
  3. 整页Save创建UPLOAD指令（3600秒），详情展示"1小时（省电模式）（设置中）"。

## [P2] 验证弹层四选项与自定义分钟值
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，进入编辑态频率弹层。
- 测试步骤
  1. 查看弹层选项构成。
  2. 选择Custom输入30并确认保存。
  3. 当前确认值分别为1、10、60分钟时重新打开弹层查看选中态。
- 预期结果
  1. 弹层为1分钟、10分钟、60分钟、自定义四个选项。
  2. 自定义30分钟保存为"自定义模式（30分钟）"，UPLOAD参数1800秒。
  3. 确认值1/10/60打开弹层自动选中对应预设，其他合法值选中Custom并填入N。

## [P4] 验证自定义分钟值边界
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，编辑态频率弹层Custom输入框可用。
- 测试步骤
  1. 输入1保存。
  2. 输入1440保存。
  3. 分别输入0、1441、30.5、空值、非数字"abc"保存。
- 预期结果
  1. 1分钟边界保存成功（映射60秒）。
  2. 1440分钟上边界保存成功（86400秒）。
  3. 0、1441、小数、空、非整数均被拦截，提示合法范围。

## [P1] 验证离线设备频率置灰不禁资料编辑
- 测试类型
  - 功能
- 前置条件
  1. P-003离线，备注"旧名"。
- 测试步骤
  1. 进入P-003详情编辑态查看Location Interval行。
  2. 修改Device Name为"新名"并整页保存。
- 预期结果
  1. 频率行置灰显示"设备离线，无法下发指令"，不能打开弹层。
  2. 普通资料仍可编辑保存成功，离线只禁频率入口。

## [P3][反向] 验证编辑中途设备离线频率独立拦截
- 测试类型
  - 功能
- 前置条件
  1. P-201在线时进入编辑态并选择频率目标60分钟。
  2. 点击整页Save前设备变为离线。
- 测试步骤
  1. 点击整页Save。
  2. 查看资料与频率两部分结果。
- 预期结果
  1. 资料接口独立提交成功。
  2. 频率接口按普通指令入口重新校验离线被拦截，展示"频率未更新"与原因，不创建静默等待上线任务。

## [P2] 验证SENDING后掉线按队列执行
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，频率目标已通过在线校验进入SENDING。
  2. 随后设备离线。
- 测试步骤
  1. 查看详情页频率展示。
  2. 等待设备重新上线后观察指令流转。
- 预期结果
  1. 详情维持目标值+"设置中"，不回退为入口离线失败。
  2. 继续按网关队列式指令逻辑执行，恢复在线后按队列投递。

## [P3][反向] 验证频率失败后不自动补发
- 测试类型
  - 功能
- 前置条件
  1. P-201某条频率指令FAILURE（如网关失败）。
- 测试步骤
  1. 设备恢复在线后等待并刷新详情。
  2. 通过指令记录页对该指令点击重新发送。
- 预期结果
  1. 恢复在线不自动补发，详情恢复最后确认值或"请选择定位频率"。
  2. 只能通过记录页重发或重新进入详情选择目标保存，两种途径均可创建新UPLOAD。

## [P2] 验证退出编辑丢弃全部本地修改
- 测试类型
  - 易用性
- 前置条件
  1. P-201编辑态已修改名称、联系人并暂存频率目标60分钟。
- 测试步骤
  1. 不保存直接返回/取消编辑。
  2. 重新进入查看态与编辑态。
- 预期结果
  1. 直接丢弃全部本地修改，不弹二次确认。
  2. 名称、联系人、电话、分组及频率暂存全部恢复原值，无任何变更落库或指令产生。


## POINT-004 定位频率数据模型与状态流转

## [P1] 验证列表与详情的频率展示分工
- 测试类型
  - 功能
- 前置条件
  1. P-201最后确认值10分钟，新提交60分钟目标进入SENDING。
- 测试步骤
  1. 查看设备列表卡片第三行。
  2. 进入P-201详情查看Location Interval。
- 预期结果
  1. 列表卡片仍展示"10分钟定位（默认模式）"（最后确认值），不把目标值冒充已生效值。
  2. 详情展示"1小时（省电模式）（设置中）"（最新在途目标）。

## [P1] 验证后端按commandId判定成功后更新确认值
- 测试类型
  - 功能
- 前置条件
  1. P-201无最后确认值，在线提交目标30分钟，生成commandId=C201。
- 测试步骤
  1. 提交后立即查看列表卡片与详情。
  2. 等待设备反馈并由后端按C201判定成功。
  3. 再次查看列表与详情。
- 预期结果
  1. 提交后卡片隐藏定位频率，详情展示"自定义模式（30分钟）（设置中）"。
  2. SENDING/SENT阶段不更新确认值。
  3. 判定成功后最后确认值更新为30分钟，卡片展示"自定义模式（30分钟）"，详情去掉"设置中"。

## [P3][反向] 验证受理与SENT不等于生效
- 测试类型
  - 功能
- 前置条件
  1. P-201最后确认值10分钟，提交60分钟目标，指令处于SENT（TCP写出）但无设备成功反馈。
- 测试步骤
  1. 查看列表卡片第三行。
  2. 查看详情页。
- 预期结果
  1. 卡片仍展示"10分钟定位（默认模式）"。
  2. 详情仍展示"1小时（省电模式）（设置中）"，平台受理与TCP写出均不覆盖最后确认值。

## [P2] 验证设置中状态持久化恢复
- 测试类型
  - 可靠性
- 前置条件
  1. P-201存在在途目标60分钟（SENDING）。
- 测试步骤
  1. 退出详情页并杀掉APP进程重启。
  2. 重新登录进入P-201详情。
- 预期结果
  1. 设置中状态由服务端持久化。
  2. 重启后详情仍恢复展示"1小时（省电模式）（设置中）"与任务状态。

## [P2] 验证FAILURE后回退展示
- 测试类型
  - 功能
- 前置条件
  1. P-201最后确认值10分钟，最新在途目标60分钟指令FAILURE（复检失败）。
- 测试步骤
  1. 查看详情页Location Interval。
  2. 查看列表卡片。
- 预期结果
  1. 无其他在途目标时详情恢复"10分钟（默认模式）"，并提示本次设置失败。
  2. 卡片保持最后确认值10分钟不变。

## [P2] 验证多在途指令后发优先与旧回执不覆盖
- 测试类型
  - 功能
- 前置条件
  1. P-201先后提交两条目标：30分钟（C301）、60分钟（C302），C301、C302均在途。
- 测试步骤
  1. 查看详情页展示。
  2. 先收到C301成功反馈，再收到C302成功反馈。
  3. 查看最终详情与卡片。
- 预期结果
  1. 详情只展示最新在途目标"1小时（省电模式）（设置中）"。
  2. C301成功更新确认值为30分钟，但详情仍展示C302目标+设置中；旧回执不覆盖最新目标。
  3. C302成功后确认值更新为60分钟，详情恢复"1小时（省电模式）"。

## [P5] 验证CANCEL回写SENT维持设置中
- 测试类型
  - 功能
- 前置条件
  1. P-201在途目标60分钟指令被用户取消，但取消时指令已写出，记录回写SENT（乐观取消回跳）。
- 测试步骤
  1. 查看详情页展示。
  2. 等待后端按commandId的后续反馈。
- 预期结果
  1. 详情维持"1小时（省电模式）（设置中）"，不回退。
  2. 继续等后端判定反馈后再按结果流转。

## [P2] 验证指令记录页重发产生新目标
- 测试类型
  - 功能
- 前置条件
  1. P-201存在历史频率指令记录（任意状态）。
- 测试步骤
  1. 进入指令记录页对该UPLOAD记录点击重新发送。
  2. 查看详情页。
- 预期结果
  1. 沿用普通指令重发规则，产生新的底层commandId。
  2. 详情最新目标重新进入"设置中"。

## [P2] 验证设置中仍可提交新目标
- 测试类型
  - 功能
- 前置条件
  1. P-201存在在途目标30分钟（SENDING）。
- 测试步骤
  1. 不等待在途指令完成，再次进入编辑选择60分钟并整页保存。
  2. 查看详情与指令记录。
- 预期结果
  1. 新目标创建成功（后发优先，无单任务锁）。
  2. 详情切换为60分钟+设置中，两条指令按各自commandId独立流转。


## POINT-005 整页保存部分成功与防重复

## [P1] 验证仅修改普通资料不产生频率指令
- 测试类型
  - 功能
- 前置条件
  1. P-201最后确认值10分钟。
  2. 编辑修改Device Name为"新名称"。
- 测试步骤
  1. 不打开频率弹层，点击整页Save。
  2. 查看详情回显与指令记录。
- 预期结果
  1. 资料保存成功退出编辑，名称更新，频率保持10分钟。
  2. 不创建任何UPLOAD指令，表单中存在频率字段不引起重复下发。

## [P1] 验证资料与频率并行保存各自成功
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，最后确认值10分钟。
- 测试步骤
  1. 编辑名称为"并行测试"并选择频率目标60分钟。
  2. 点击整页Save。
  3. 查看详情与指令记录。
- 预期结果
  1. 资料接口与频率接口独立并行提交。
  2. 名称更新为"并行测试"；频率进入SENDING，详情展示目标值+设置中。
  3. APP不等待终端ACK即退出编辑，两部分结果分别回显。

## [P3][反向] 验证资料成功频率失败不互相回滚
- 测试类型
  - 功能
- 前置条件
  1. P-201在线，mock频率接口返回明确拒绝。
- 测试步骤
  1. 编辑名称为"部分成功"并选择频率目标，点击整页Save。
  2. 查看名称与频率展示。
- 预期结果
  1. 资料保存成功，名称"部分成功"生效，不因频率失败回滚。
  2. 频率保持最后确认值（或"请选择定位频率"），明确提示"频率未更新"及原因。

## [P3][反向] 验证资料失败频率成功不互相回滚
- 测试类型
  - 功能
- 前置条件
  1. mock资料接口返回失败，频率链路正常。
- 测试步骤
  1. 编辑资料并选择频率目标，点击整页Save。
  2. 查看编辑页与频率状态。
- 预期结果
  1. 资料失败保留编辑内容并展示失败原因。
  2. 已创建的频率指令不被取消，详情/记录页可查其独立流转。

## [P2] 验证Save按钮防重复提交
- 测试类型
  - 功能
- 前置条件
  1. P-201编辑态已修改资料并选择频率目标。
- 测试步骤
  1. 点击整页Save后立即连续快速点击3次。
  2. 查看指令记录与服务端指令数。
- 预期结果
  1. 首次点击后按钮loading并禁用，完成当前响应前不可再点击。
  2. 只产生一条频率UPLOAD，一次用户操作最多一条指令。

## [P2] 验证幂等键防重复指令
- 测试类型
  - 可靠性
- 前置条件
  1. P-201编辑态，已选择频率目标30分钟。
- 测试步骤
  1. 点击Save后模拟网络超时（服务端已受理但响应未达）。
  2. 弱网重连后APP恢复处理。
- 预期结果
  1. 客户端超时、服务端结果未知时不自动新建指令。
  2. 按已有commandId查询原记录，按四态结果恢复页面；同operationId幂等键服务端去重，不产生重复UPLOAD。

## [P2] 验证相同目标值重复确认仍创建指令
- 测试类型
  - 功能
- 前置条件
  1. P-201最后确认值10分钟，无在途目标。
- 测试步骤
  1. 选择与确认值相同的10分钟目标并整页保存。
  2. 再次选择10分钟保存。
- 预期结果
  1. 用户明确确认仍创建新UPLOAD（重新确认终端配置）。
  2. 第二次同样创建新指令，不以设备+秒数去重误伤合法设置意图。

## [P2] 验证跨账户共享字段同步与分组独立
- 测试类型
  - 功能
- 前置条件
  1. alice01与owner01双绑P-202：alice01侧分组"广东省"，owner01侧"企业A组"。
- 测试步骤
  1. alice01修改名称"共享牛"、联系人"李四"、电话+8613912345678并改组"浙江省"保存。
  2. owner01端刷新查看P-202。
- 预期结果
  1. alice01保存成功。
  2. 名称、联系人、电话跨账户同步为新值（设备级共享字段）；owner01侧分组仍为"企业A组"不受影响，Group为账户级关系字段。

## [P5] 验证后写覆盖并发修改
- 测试类型
  - 功能
- 前置条件
  1. alice01与owner01同时编辑P-202名称。
- 测试步骤
  1. alice01保存名称"版本A"。
  2. owner01随后保存名称"版本B"。
- 预期结果
  1. 两次保存均成功，名称先为"版本A"。
  2. 最终名称为"版本B"（后写覆盖先写，不做版本冲突拦截），两端刷新后以服务端最终值为准。

# US-019 设备入口的告警与指令


## POINT-001 设备入口告警处理

## [P1] 验证未处理告警打开处理弹窗备注必填
- 测试类型
  - 功能
- 前置条件
  1. P-201存在未处理告警AL-20260922-01。
- 测试步骤
  1. 从设备入口进入告警记录，点击AL-20260922-01。
  2. 尝试不填备注直接提交处理。
  3. 输入"  已现场核实  "（首尾空格）提交。
- 预期结果
  1. 打开处理弹窗，备注为必填项。
  2. 备注为空或纯空格被拦截提交。
  3. 去空格后"已现场核实"保存成功，≤200字符约束生效。

## [P4] 验证处理备注长度边界
- 测试类型
  - 功能
- 前置条件
  1. P-201存在未处理告警。
  2. 准备200字符与201字符备注文本。
- 测试步骤
  1. 输入200字符备注提交处理。
  2. 输入201字符备注提交。
- 预期结果
  1. 200字符上点保存成功。
  2. 201字符离点被拦截，提示长度限制。

## [P2] 验证已处理告警打开只读详情卡片
- 测试类型
  - 功能
- 前置条件
  1. P-201存在已处理告警AL-20260920-01（处理备注"已电话确认"）。
- 测试步骤
  1. 从设备入口进入告警记录点击该已处理记录。
  2. 查看卡片内容与可用操作。
- 预期结果
  1. 打开只读详情卡片，可查看告警详情与处理备注"已电话确认"。
  2. 不提供再次编辑处理的入口。

## [P3][反向] 验证告警中心历史报警仅只读
- 测试类型
  - 功能
- 前置条件
  1. 告警中心存在P-201相关告警卡片。
- 测试步骤
  1. 从告警中心卡片进入历史报警页面。
  2. 查找处理按钮、批量处理入口、页签与筛选。
- 预期结果
  1. 历史报警页面仅只读查询。
  2. 无处理与批量入口，不继承告警中心的页签与筛选，与设备入口告警页不互相替代。

## [P2] 验证两个告警页面数据同源
- 测试类型
  - 功能
- 前置条件
  1. P-201存在未处理告警AL-20260922-01。
- 测试步骤
  1. 从设备入口处理AL-20260922-01并填写备注。
  2. 从告警中心进入P-201历史报警查看同一条。
- 预期结果
  1. 设备入口处理成功。
  2. 告警中心侧记录同步为已处理，两处读取同源告警记录，状态一致。

## [P3][反向] 验证设备关系失效拦截越权处理
- 测试类型
  - 安全性
- 前置条件
  1. alice01已进入P-201告警记录页。
  2. 另一端解除alice01与P-201绑定。
- 测试步骤
  1. 在页面停留期间对未处理告警提交处理。
  2. 查看处理请求结果与告警状态。
- 预期结果
  1. 服务端按最新权限校验失败。
  2. 处理不落库，不产生越权处理副作用。


## POINT-002 设备入口指令入口规则

## [P1] 验证各状态设备均可进入指令详情页
- 测试类型
  - 功能
- 前置条件
  1. P-001在线、P-003离线、P-004未连接、P-005告警，均属alice01可见。
- 测试步骤
  1. 分别从四台设备更多菜单点击Send Command。
  2. 查看跳转页面。
- 预期结果
  1. 四种状态均成功进入对应设备的指令详情页。
  2. 跳转目标不因通信状态不同改变，入口不区分在线、离线、未连接或告警。

## [P1] 验证指令详情页携带设备唯一标识
- 测试类型
  - 功能
- 前置条件
  1. P-001与P-002备注均为"牛群一号"。
- 测试步骤
  1. 从P-001更多菜单进入Send Command。
  2. 从P-002更多菜单进入Send Command。
  3. 对比两页的设备身份与历史记录。
- 预期结果
  1. P-001指令详情页以P-001唯一标识查询。
  2. P-002指令详情页以P-002唯一标识查询。
  3. 同名设备不串页，历史指令记录各归各设备。

## [P2] 验证历史指令记录各状态可查看
- 测试类型
  - 功能
- 前置条件
  1. P-003离线但有历史指令记录3条。
- 测试步骤
  1. 从P-003更多菜单进入Send Command。
  2. 查看历史指令记录。
- 预期结果
  1. 正常进入页面。
  2. 3条历史记录可查看，离线状态不阻断历史记录查看。

## [P2] 验证具体指令下发沿用模块规则
- 测试类型
  - 功能
- 前置条件
  入口设备P-001在线，指令详情页正常。
- 测试步骤
  1. 在指令详情页选择具体指令提交下发。
  2. 查看下发闭环。
- 预期结果
  1. 下发前置判断、参数校验沿用单设备指令下发模块规则。
  2. 提交结果与执行闭环正常，本入口不前置额外拦截。

## [P3][反向] 验证设备失权拦截越权指令记录
- 测试类型
  - 安全性
- 前置条件
  1. alice01已进入P-001指令详情页。
  2. 另一端解除alice01与P-001绑定。
- 测试步骤
  1. 停留页面继续查看历史指令记录或发起下发。
  2. 查看页面返回结果。
- 预期结果
  1. 按统一权限与数据访问规则处理。
  2. 不展示越权指令记录，下发请求被服务端拒绝。

# US-032 新设备类型接入与跨端一致性


## POINT-001 新类型接入与存量回归

## [P1] 验证牛马定位器完整接入链路
- 测试类型
  - 功能
- 前置条件
  1. 管理后台已入库牛马定位器P-300，类型字段维护为牛马定位器。
- 测试步骤
  1. 依次执行：库存查询 → alice01绑定 → 归入分组 → 设备列表展示 → 详情/物联卡查询。
  2. 核对链路各环节数据一致性。
- 预期结果
  1. 库存→绑定→分组→列表→查询完整走通。
  2. 各环节设备身份、类型、归属一致，不只改显示名称。

## [P1] 验证三端设备口径一致
- 测试类型
  - 兼容性
- 前置条件
  1. P-300已绑定alice01，当前在线，SIM在用，服务到期2027-06-30。
- 测试步骤
  1. 分别在APP、监控平台Web、管理后台查看P-300的账号归属、设备归属、分组、SIM状态、在线状态、到期时间。
  2. 逐项对比三端数据。
- 预期结果
  1. 三端均可正常查看。
  2. 账号、设备归属、分组、SIM状态、在线状态、到期时间三端一致，无口径漂移。

## [P2] 验证旧设备类型规则不变
- 测试类型
  - 兼容性
- 前置条件
  1. 平台存量为旧类型设备（如北斗终端）及其围栏、告警、历史分页。
- 测试步骤
  1. 牛马定位器类型上线后，回归旧类型设备的绑定、围栏判定、告警同步、历史分页。
  2. 对比上线前后旧类型业务行为。
- 预期结果
  1. 旧类型的绑定、围栏、告警、指令规则不变。
  2. 旧多边形围栏、原告警同步、原历史分页无规则变化，存量业务无回归。

## [P3][反向] 验证APP不支持旧类型绑定
- 测试类型
  - 功能
- 前置条件
  1. P-200为已入库的北斗终端。
- 测试步骤
  1. alice01在APP发起绑定输入P-200卡号。
  2. 查看绑定结果与提示。
- 预期结果
  1. 绑定被拦截。
  2. 提示"当前设备类型暂不支持"，APP当前仅支持牛马定位器。

## [P2] 验证协议不兼容时只隔离适配边界
- 测试类型
  - 可扩展性
- 前置条件
  1. 存在协议差异的牛马定位器新型号P-301。
- 测试步骤
  1. P-301入库并绑定后查看定位、告警、指令。
  2. 对比原平台业务流程。
- 预期结果
  1. 补齐该类型必要的数据与指令适配后业务走通。
  2. 沿用原权限、查询与记录链，不新建授权入口或独立设备事实库，只隔离必要的协议适配边界。
