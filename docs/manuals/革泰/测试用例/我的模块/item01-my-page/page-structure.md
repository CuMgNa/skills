# 用户故事：我的页三区域结构与导航

> 口径依据：大纲第 2、3 章。我的页为 Tab 根页面（无返回按钮）；三区域结构（头部卡片 / 账号选项 / 设备设置）+ 底部退出登录；More 层废弃（Language、Offline Map 为 UI 错误不存在，在线刷新间隔直平区域三）；系统消息入口已移除；各导航切换保留页面状态；二级页返回回「我的」恢复原状态。

## [P1] 验证我的页三区域结构与入口完整性
- 测试类型
  - 功能
- 前置条件
  1. 账号 testuser01（密码 Test@1234）已登录
- 测试步骤
  1. 点击底部导航「我的」Tab，查看页面整体结构。
  2. 核对区域一（头部卡片：头像 + 账号名 + 登录标识）、区域二（资料 / 修改密码 / 通知提醒）、区域三（指令记录 / 时区设置 / 在线刷新间隔）、底部退出登录按钮。
  3. 检查是否存在 More 入口、Language、Offline Map、系统消息入口。
  4. 检查「账号选项」「设备设置」两个分组标题的交互。
- 预期结果
  1. 我的页正常展示，为 Tab 根页面（左上角无返回按钮）。
  2. 三区域 + 底部共 6 个菜单项与退出登录按钮全部到位。
  3. 不存在 More 层、Language、Offline Map、系统消息入口（More 层废弃拍板）。
  4. 分组标题仅作视觉分区，点击无任何响应。

## [P2] 验证各二级页导航与返回恢复我的页状态
- 测试类型
  - 功能
- 前置条件
  1. 账号 testuser01 已登录，我的页可滚动
- 测试步骤
  1. 在我的页将列表滚动到中部位置，记录滚动状态。
  2. 依次点击区域二、区域三各菜单项进入二级页，再逐个左上返回。
  3. 每次返回后检查我的页滚动位置与展示状态。
  4. 切换到「地图」Tab 后再切回「我的」Tab，检查页面状态。
- 预期结果
  1. 滚动状态记录成功。
  2. 6 个二级页均可正常进入并返回「我的」。
  3. 每次返回我的页恢复原状态（滚动位置不重置）。
  4. Tab 切换再回来，我的页状态保留（导航切换保留当前页面状态）。

## [P2] 验证底部退出登录按钮样式与入口
- 测试类型
  - 功能
- 前置条件
  1. 账号 testuser01 已登录
- 测试步骤
  1. 查看底部退出登录按钮样式。
  2. 点击退出登录按钮。
  3. 观察退出流程触发（具体流程归 US-031 登录篇，此处仅断言入口行为）。
- 预期结果
  1. 按钮为描边样式，位于页面底部。
  2. 点击后触发退出登录流程（确认弹窗 / 直退以登录篇为准）。
  3. 退出后回登录页（链路细节按登录篇 US-031 断言）。

## [P5] 验证资料页与我的页的头像双入口进同一页面
- 测试类型
  - 功能
- 前置条件
  1. 账号 testuser01 已登录
- 测试步骤
  1. 点击我的页区域二「资料」菜单项，进入页面并记录标题。
  2. 返回我的页，点击区域一头部头像 / 头部区域。
  3. 比较两个入口进入的页面。
- 预期结果
  1. 「资料」入口进入资料页。
  2. 点头像也进入资料页（导流入口）。
  3. 两入口进入同一页面（双入口同页）。
