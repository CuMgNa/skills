# 联调功能全量回归测试计划（2026-09-10）

体例对齐 [联调功能全量回归测试报告-2026-09-08.md](联调功能全量回归测试报告-2026-09-08.md)。

**分场，不混测：**

| 场 | 范围 | 本文件状态 |
|----|------|------------|
| **A 场（本场已跑）** | UDP 透传。中转开着，顶栏「已联网」`net=2`。测完未关 UDP、未跑手册 §10。 | 已填结果 |
| **B 场（后面另测）** | 非透传。关 `$TIREL,0`、ABCD 的 A/C、无 socket `$TOLGN,0`、关中转推 `$TILOC,0`、`net!=2` 排队。 | 未开测 |

---

## 联调结论（A 场）

- **Gate：** G2 / G4 透传子集 / G5 透传 F 项 **通过**。G1、G3 **B 场未测**。
- **未通过项：** 无（无 Gate 失败）。
- **单元测试：** `:app:testDebugUnitTest` **32 suite / 127 用例 / 0 失败**；`:rescuesat-ble-core:test` **9 suite / 63 用例 / 0 失败**。
- **收尾：** 按计划 **未** 关 UDP、**未** 手册 §10。终端保持上电，App 保持已联网。

| 门禁 | 结果 | 本场证据 |
|------|------|----------|
| 单测 | **通过** | App 127/0；ble-core 63/0 |
| 开 UDP | **通过** | 指令调试「已开启 · 目标 120.77.17.225:10306 · 剩余 7 天」；`$TIREL,1` → `$TOREL,1` |
| 已联网 | **通过** | 顶栏「202606030046 · 已联网」；`$TOSTA,202606030046,100,0,1,2`（`net=2`） |
| G2 | **通过** | 发 `a10g2` → `$TITXT,32801,5,6131306732` → `$TODST,32801,1` → `$TOACK` / `$TODST,32801,4`；气泡「发送成功」；发送详情 5 字节 / 回执 829 ms |
| G1 | **B 场** | 未跑 `relay_abcd_test.py` |
| G3 | **B 场** | 未关 UDP |

### 本场观察（不阻塞 A 场）

- 空闲心跳在中转刚开时有 `$TILGN,0` → **`$TOLGN,1`**。其后 `$TOSIG` 约 0.5s 一条，心跳多回 **`$TOLGN,2`（上行中）**，属协议状态码，不是无回执。
- `SIMVOI` 下行气泡为「无效语音 / 语音解码失败（20007）」；`SIMVOIMISS` 为「缺失 1 包」。模拟下行，不判 Gate 失败。
- 聊天图「800 B」点标签/缩略区域，uiautomator 未见独立预览层（F1 记未测）。长按有「重发/删除」，发送详情「图片 · 1 KB」四包回执齐全。

---

## 环境

| 项 | 值 |
|----|-----|
| 日期 | 2026-09-10（约 16:43–17:35） |
| 手机 | `E6U8QCRWSOPZMJWC`（OPPO PEMT20 / Android 13） |
| App | `com.xinglian.weishi` debug |
| 终端 | `DC:32:62:53:03:44` / `PD_202606030046` |
| 平台 | `120.77.17.225:10306` |
| 启动 | App 已在前台 `MainActivity`，未 `am start` |

## 结果图例

| 标记 | 含义 |
|------|------|
| **通过** | 本轮有明确操作 / 日志 / 脚本输出证据 |
| **未测** | 本轮未能执行，原因如实写明（非跳过） |
| **不适用** | 当前状态下控件本应隐藏或不可达，行为符合设计 |
| **失败** | 有证据表明行为不符合预期 |
| **B 场** | 依赖关中转 / 未开中转，本场不测 |

## A 场故意不点

BLE/UDP **清空**、`$TIADD,1`（仅选中）、`$TITSM,0`、**`$TIREL,0`**、选图页真发（用 `SIMTIIMG` 的 800 B 气泡验收）、**断开确认**（只测取消，以免拆掉本场已联网）。

---

# A 场：UDP 透传

## A.0 开场与门禁

| 门禁 | 操作 | 期望 | 结果 | 证据 |
|------|------|------|------|------|
| 单测 | `:app:testDebugUnitTest` + `:rescuesat-ble-core:test` | BUILD SUCCESSFUL，0 失败 | **通过** | 127 + 63，0 失败 |
| 开 UDP | 指令调试看中转 | 开关「已开启…」 | **通过** | 「已开启 · 目标 120.77.17.225:10306 · 剩余 7 天」 |
| 已联网 | 消息/地图顶栏 | 「已联网」；`net=2` | **通过** | `connection_status`；`$TOSTA … 1,2` |
| G2 | 发 `a10g2` | TODST 4 + 发送成功 | **通过** | seq 32801；详情回执 829 ms |
| G1 | 整本 ABCD | **B 场** | B 场 |  |
| G3 | `$TIREL,0` | **B 场** | B 场 |  |

---

## 一、交付功能专项（A 场）

| # | 功能 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| F1 | 聊天图片点击预览 | 点 800 B 气泡 | 出现预览后可关闭 | **未测** | 点标签与缩略区域，dump 无独立预览层 |
| F2 | 无定位芯片「无位置」 | 地图页 | 「无位置」 | **通过** | `map_location_status_text` =「无位置」 |
| F3 | 关中转推 `$TILOC,0` | — | **B 场** | B 场 |  |
| F4 | SIMTLOC | 快捷发送 | `$TOLOC,1,…` | **通过** | `$TOLOC,1,39.904987,116.407394,1789032480` |
| F5 | SOS 顶栏 | 看消息/地图 | 无则不适用 | **不适用** | 无 SOS 节点 |
| F6 | 关 UDP 无残留 | — | **B 场** | B 场 |  |
| F7 | 发送详情 | 点状态行 | 过程 + 本次设置 | **通过** | 文本/语音/图片三套均打开 BottomSheet |
| F8 | 服务器配置进出页 | 同 N1 | 不卡「读取中…」 | **通过** | 设置页一直 `120.77.17.225:10306`；消息回设置仍正确 |
| F9 | 30s 心跳（有 socket） | `$TILGN,0` | `$TOLGN,1` | **通过** | 当日有 `$TOLGN,1`；忙时 `$TOLGN,2` |

---

## 二、固件 ABCD（A 场不跑）

| 组 | 编号 | 用例 | 本场 | 结果 |
|----|------|------|------|------|
| A | A1–A6 | 未开中转 | B 场 | B 场 |
| B | B1–B5 | 开中转 / 过线 | App 路径代替 | **通过** | G2 + `$TIGAT,1`→`$TOGAT,1` + `$TILOC,0` |
| B | B6/B7 | 先连再开 | App 已连已开 | **通过** | 开场即已联网 |
| C | C1–C7 | 关/再开 | B 场 | B 场 |
| D | D1 | `TIADD,0` | A 场 N1 | **通过** | `$TOADD,120.77.17.225,10306` |
| D | D2–D4 | 中途关 / 断 30s | B 场 | B 场 |
| D | D5 | 断开确认 | 只测取消 | **通过** | 取消后仍「蓝牙已连接」 |
| D | D6 | 30s 内重连 | 本场未确认断开 | **未测** | 保本场已联网，不断开 |

---

## 三、App 全量控件清单（A 场）

### 3.1 启动与引导

| # | 页面 | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|------|
| 1 | Splash | `splash_root` | 冷启动 | 自动跳转 | **不适用** | 已在主界面 |
| 2 | 引导页 | 「开始使用」 | 点击 | 进主界面 | **不适用** | 已授权 |
| 3 | 引导页 | 权限列表 | 展示 | — | **不适用** | 非首次 |

### 3.2 主导航

| # | 页面 | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|------|
| 4 | 底栏 | 消息 | 点击 | 进消息页 | **通过** | `nav_item_messages` |
| 5 | 底栏 | 地图 | 点击 | 进地图页 | **通过** | 顶栏「地图」+「已联网」 |
| 6 | 底栏 | 设置 | 点击 | 进设置页 | **通过** | 「服务器配置」可见 |
| 7 | 工具栏 | 返回 | 点击 | 授权详情回设置 | **通过** | 授权详情 BACK → 授权列表 → 设置 |
| 8 | 录音 | 按住说话 | 长按 | 进入录音 | **通过** | 先出麦克风权限；授权后 2.2s 出现「正在压缩语音…」 |
| 9 | 录音 | 松手 | 松手 | 出语音气泡 | **通过** | 「2 ″」「发送成功」17:15 |
| 10 | 录音 | 取消区 | 滑入 | 松手取消 | **未测** | 未滑入取消区（避免再占上行） |
| 11 | 录音 | sheet | 触摸 | overlay | **通过** | `hold_to_talk` 可见并可 long-press |

### 3.3 消息页

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 12 | 顶栏状态 | 展示 | 「已联网」 | **通过** | `202606030046 · 已联网` |
| 13 | 顶栏图标 | 展示 | 蓝牙图标 | **通过** | `bluetooth_status_icon` |
| 14 | 搜星转圈 | 展示 | 已联网可隐 | **不适用** | 已联网，无 searching 节点 |
| 15 | 终端标识 | 展示 | 可见 | **通过** | 顶栏含 `202606030046` |
| 16 | SOS | 展示 | 无则不适用 | **不适用** | 节点不存在 |
| 17 | 电量 | 展示 | 有 battery | **通过** | `100` |
| 18 | 信号 | 展示 | 有 signal | **通过** | `signal_status_icon` |
| 19 | 列表滚动 | swipe | 可滚 | **通过** | swipe `message_list` |
| 20–24 | 常用语 5 条 | 点击 | 填入输入框 | **通过** | 五条均填入 |
| 25–26 | 常用语关/开 | 点入口 | 可再开 | **通过** | 点一条后 sheet 关，再开继续点 |
| 27 | 输入模式 | 点击 | 切语音/文本 | **通过** | 「按住说话」↔ 输入框 |
| 28 | 输入框 | 输入 | 可输入 | **通过** | 打出 `a10g2` |
| 29 | 按住说话 | 长按 | 录音 | **通过** | 同 #8 |
| 30 | 选图 | 点击 | 打开选图 | **通过** | 「选择图片」；先过相册权限 |
| 31 | 发送 | 点击 | 出站成功 | **通过** | `a10g2` 发送成功 |
| 32 | 文本气泡 | 点击 | 无异常 | **通过** | 点 `ldok` / `a10g2` 状态行 |
| 33 | 文本气泡 | 长按 | 重发/删除 | **通过** | `ldok` 长按「重发 \| 删除」 |
| 34 | 语音气泡 | 点击 | 播放 | **通过** | 点「2 ″」无崩 |
| 35 | 语音气泡 | 长按 | 重发/删除 | **通过** | 「重发 \| 删除」 |
| 36 | 图片气泡 | 点击 | 预览 | **未测** | 见 F1 |
| 37 | 图片预览 | 关闭 | 回列表 | **未测** | 预览层未出现 |
| 38 | 图片气泡 | 长按 | 重发/删除 | **通过** | 「800 B」长按「重发 \| 删除」 |
| 39 | 发送失败条 | 点击 | 无则不适用 | **不适用** | 本场出站无失败条（下行「无效语音」不是出站失败） |

### 3.4 地图页

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 40 | `map_connection_bar` | 展示 | 已联网 | **通过** | 「202606030046 · 已联网」 |
| 41 | SOS | 展示 | 无则不适用 | **不适用** | 无 SOS |
| 42 | 下载 | 点击 | 进离线地图 | **通过** | 「离线地图 \| 输入城市名称…」；BACK 回地图 |
| 43 | 图层 | 点击 | 有响应 | **通过** | tap `map_type_button` 无崩 |
| 44 | 定位 | 点击 | 有响应 | **通过** | tap `map_locate_button` |
| 45 | 放大 | 点击 | 有响应 | **通过** | `map_zoom_in_button` |
| 46 | 缩小 | 点击 | 有响应 | **通过** | `map_zoom_out_button` |
| 47 | 坐标卡片 | 点击 | 无坐标则隐 | **不适用** | 无有效坐标 |
| 48 | 位置芯片 | 文案 | 无位置 | **通过** | 「无位置」 |
| 49 | 空态 | 点击 | 已连则不适用 | **不适用** | 已显示地图 |
| 50 | 手势 | 拖动 | 可平移 | **通过** | swipe `map_container` |

### 3.5 设置页

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 51 | 更换设备 | 点击后取消 | 确认框取消 | **通过** | 「更换蓝牙设备…」→ 取消，仍已连接 |
| 52 | 授权信息 | 点击 | 进授权页 | **通过** | 「语音库授权信息 / 永久有效」 |
| 53 | 指令调试 | 点击 | 进调试；开关已开 | **通过** | UDP「已开启…7 天」 |
| 54 | 服务器配置 | 点击 | 开对话框 | **通过** | IP `120.77.17.225` 端口 `10306` |
| 55 | 服务器对话框 | 取消 | 不改值 | **通过** | 取消后右侧仍原地址 |
| 56 | 发送配置 → 超时 | 打开 | 五档可见 | **通过** | 15秒（联调）/1/5/10/30 分钟 |
| 57 | 超时选择器 | 取消 / 改回 | 默认 10 分钟 | **通过** | 改 15 秒再改回 10 分钟 |
| 58 | 版本 | 点击 | 有响应 | **通过** | `V1.0.0` 已点 |
| 59 | 断开 | 取消 | 保持已连接 | **通过** | 「断开蓝牙连接」→ 取消 |
| 60 | 断开 | 确认 | 未连接后重连 | **未测** | 故意不确认，保本场已联网 |

### 3.6 授权管理

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 61 | 授权入口 | 点击 | 进列表 | **通过** | 语音/图像两张卡 |
| 62 | 授权卡 | 展示 | 可见 | **通过** | 永久有效 |
| 63 | 详情 | 点语音卡 | 授权详情 | **通过** | 「授权详情 \| 语音库授权信息 \| 到期时间 永久有效 \| 总调用次数 不限」 |
| 64–65 | 激活 | — | 已永久有效 | **不适用** | 无激活框 |
| 66–67 | 解绑 | 入口后取消 | 不解绑 | **未测** | 本页未见 `authorization_unbind_all`（未再滚找） |

### 3.7 指令调试

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 68 | UDP 开关 | 只验已开 | 「已开启…」 | **通过** | 目标 120.77.17.225:10306 · 剩余 7 天 |
| 68b | UDP 关 | — | **B 场** | B 场 |  |
| 69 | 快捷 Spinner | 点击 | 展开 | **通过** | 见 QUICK_ITEMS |
| 70 | 发送 | 点击 | 发出 | **通过** | 多条 `$TI*` 入 jsonl |
| 71 | BLE 日期 | 点击 | 可切天数 | **通过** | 「今天 \| 近 2 天 \| 近 3 天 \| 近 7 天」 |
| 72 | BLE 筛选 | 打开后关 | 可关 | **通过** | 已点 |
| 73 | BLE 暂停/跟随 | 点两次 | 暂停再恢复 | **通过** | `button_toggle_debug_log` |
| 74 | BLE 复制 | 点击 | 有响应 | **通过** | `button_copy_debug_log` |
| 75 | BLE 列表 | 展示 | 有 TX/RX | **通过** | `RX $TOSIG`；计数 14149 条 |
| 76 | BLE 清空 | — | 故意不点 | **未测** | 防毁证 |
| 77 | BLE 滚动 | swipe | 可滚 | **通过** | swipe `debug_log_content` |
| 78–81 | UDP 日期/筛选/暂停/复制 | 点击 | 有响应 | **通过** | UDP 日志 23–24 条 |
| 82 | UDP 清空 | — | 故意不点 | **未测** | 防毁证 |
| 83 | UDP 滚动 | swipe | 可滚 | **通过** | swipe `udp_log_content` |

**快捷指令**

| # | 指令 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 84 | SIMTTXT | 发送 | `$TOTXT` | **通过** | `$TOTXT,9999,8,C6BDCCA8B2E2CAD4` |
| 85 | SIMVOI | 发送 | `$TOVOI` | **通过** | `$TOVOI,1,1,2,…`；气泡「无效语音 / 20007」 |
| 86 | SIMTIVOI | 发送 | `$TIVOI` + TODST,4 | **通过** | `$TIVOI,32803` hex `0000F516…`；`$TODST,32803,4`；「1 ″」发送成功 |
| 87 | SIMTIIMG | 发送 | `$TIIMG` 分包 | **通过** | `$TIIMG,32804` 四包 + `$TODST,32804,4`；「800 B」 |
| 88 | SIMTLOC | 发送 | `$TOLOC,1` | **通过** | 见 F4 |
| 89 | `$TIQRY,0` | 发送 | `$TOSTA` net=2 | **通过** | `$TOSTA,202606030046,100,0,1,2` |
| 90 | `$TILOC,0` | 发送 | `$TOLOC` | **通过** | `$TOLOC,0,,,0` |
| 91 | `$TILOC,1,…` | 发送 | 写坐标 | **未测** | Spinner 未滚到该项；自定义发送被系统返回打断 |
| 92 | `$TISIG,0` | 发送 | `$TOSIG` | **通过** | 日志持续 `RX $TOSIG,0,0,0,0` |
| 93 | `$TITSM,1` | 发送 | 模块保持开 | **通过** | `$TITSM,1` → `$TOSTA … 1,2` |
| 94 | `$TIGAT,0` | 发送 | `$TOGAT` | **通过** | `$TOGAT,1` |
| 95–98 | 空包 TITXT/TIVOI/TIIMG/TIDSQ | — | — | **未测** | 改发 SIM 真包，避免再占空包上行 |
| 99 | `$TIREL,1` | 发送 | `$TOREL,1` | **通过** | `$TIREL,1` → `$TOREL,1` |
| 99b | `$TIREL,0` | — | **B 场** | B 场 |  |
| 100 | `$TIADD,0` | 发送 | `$TOADD …10306` | **通过** | `$TOADD,120.77.17.225,10306` |
| 101 | `$TIADD,1,…` | 仅选中 | 不发送 | **未测** | 已选中 `$TIADD,1,<ip>,<port>`，未点发送 |
| 102 | `$TIUDP` | 仅选中 | 不发送 | **未测** | 故意不发 |
| 103 | 自定义… | 输入发送 | 能发 | **未测** | 选中自定义后被后续返回打断，未形成有效 `$TIQRY` |

### 3.8 连接流程

| # | 页面 | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|------|
| 104–106 | 设备列表 / 扫描 | — | 本场未断 | 重连 | **未测** | 未确认断开 |
| 107 | 连接中取消 | — | 未停该页 | **不适用** |  |
| 108–109 | 失败页 | — | 未进入 | **不适用** |  |

### 3.9 选图页

| # | 控件 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| 110 | 关闭选图 | 点击 | 回消息 | **通过** | `close_image_picker` |
| 111 | 缩略图网格 | 展示 | `image_grid` | **通过** | 网格可见 |
| 112 | 空相册 | — | 非空则不适用 | **不适用** | 相册有图 |
| 113 | 大图预览 | 展示 | `image_preview` | **通过** | 「预览」按钮可见 |
| 114 | 发送图片 | 可见 | 可不点发送 | **通过** | `send_image` 可见；真图用 SIMTIIMG 的 800 B 验收 |

---

## 三续、09-10 新增（A 场）

### N1 / F8 服务器配置

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N1.1 | 进设置 | 右侧地址，非「读取中…」 | **通过** | `120.77.17.225:10306` |
| N1.2 | 进指令调试再回 | 仍显示地址 | **通过** | 调试页目标同地址；回设置仍正确 |
| N1.3 | 进消息再回 | 同上 | **通过** | 消息 → 设置仍 `120.77.17.225:10306` |
| N1.4 | jsonl | `$TIADD,0` / `$TOADD` | **通过** | `$TOADD,120.77.17.225,10306` |
| N1.5 | 对话框取消 | 不改值 | **通过** |  |
| N1.6 | 非法 IP 保存 | 「IP 或端口无效」 | **通过** | `120.77.17.225x` +「IP 或端口无效」；取消后原值 |
| N1.7 | 不改平台 | 未写 `$TIADD,1` | **通过** | 仅选中未发送 |

### N2 发送配置

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N2.1 | 进入页 | 四行可见 | **通过** | 10分钟 / 2次 / 5秒 / 240字节 |
| N2.2 | 超时 | 五档；改回 10 分钟 | **通过** | 改 15秒（联调）再改回 |
| N2.3 | 重发 | 1/2/3 次 | **通过** | 打开后取消，保持 2 次 |
| N2.4 | 回执 | 3–10 秒 | **通过** | 打开后取消，保持 5 秒 |
| N2.5 | 分包 | 200–240 | **通过** | 打开后取消，保持 240 字节 |

### N3 蓝牙配置

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N3.1 | 打开选择器 | 60/30/20/10/5 分钟 | **通过** |  |
| N3.2 | 选 5 分钟 | 右侧「5分钟」 | **通过** |  |
| N3.3 | 改回 30 分钟 | 「30分钟」 | **通过** |  |
| N3.4 | 真断 | 不坐等 | **未测** | 单测 `BleIdleDisconnectTest` 已在 App 127 内 |

### N4 图像配置

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N4.1 | 打开 | 7 档 | **通过** | 1920×1080 … 320×240 |
| N4.2 | 改一档再改回 | 512×384 | **通过** | 640×480 → 512×384 |

### N5 / F7 发送详情

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N5.1 | 文本 `ldok` / `a10g2` | 发出 + 回执 + 本次设置 | **通过** | `a10g2`：文本 · 5 字节，回执 829 ms；UDP 透传 已开启 |
| N5.2 | 复制 | Toast | **通过** | 已点 `send_trace_copy`（Toast 不进 dump） |
| N5.3 | 关闭 | 收起 | **通过** | `send_trace_close` |
| N5.4 | 语音 | 同一套 | **通过** | 「2 ″」：语音 · 2 秒 · 150 bps · 41 字节 · 回执 879 ms；另有 SIMTIVOI「1 ″」192 字节 |
| N5.5 | 图片 | 同一套 | **通过** | 「800 B」：图片 · 1 KB · 800 字节 · 4 包 · 用时 5.0 秒 |
| N5.6 | 失败条 | 本场无出站失败 | **不适用** |  |

### N6 / F9 心跳

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N6.1 | 约 30s | `$TOLGN,1` | **通过** | 当日 `$TILGN,0` → `$TOLGN,1`；忙时 `$TOLGN,2` |
| N6.2 | 快捷 `$TILGN,0` | 立刻 `$TOLGN` | **通过** | 调试发送后 `$TOLGN,2`（上行中） |
| N6.3 | 不重置闲时断链 | 对照单测 | **通过** | `BleIdleDisconnectTest` / `PlatformIdleHeartbeatTest` 在 127 内 |
| N6.4 | `$TOLGN,0` | **B 场** | B 场 | 当日日志早期有 `$TOLGN,0`（中转未开时段），不计入本场验收 |

### N7 新增快捷项

| # | 指令 | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|------|
| N7.1 | `SIMVOIMISS` | 发送 | 有 `$TO*` | **通过** | `$TOVOI,2,…`；气泡「缺失 1 包」 |
| N7.2 | `$TIGAT,1` | 发送 | `$TOGAT,1` | **通过** |  |
| N7.3 | `$TIGAT,2` | 发送 | `$TOGAT` | **通过** | `$TOGAT,1` |
| N7.4 | `$TILGN,0` | 发送 | `$TOLGN` | **通过** | 见 N6.2 |
| N7.5 | `$TIREL,0` | — | **B 场** | B 场 |  |
| N7.6 | `$TIADD,1` / `$TIUDP` | 仅选中 | 不发送 | **通过** | `$TIADD,1` 已选中未发送 |

### N8 协议日志 RX

| # | 操作 | 期望 | 结果 | 证据 |
|---|------|------|------|------|
| N8.1 | 调试页 | `RX $TOADD` / `$TOSIG` | **通过** | 交互日志可见 `RX $TOSIG` |
| N8.2 | jsonl | `"d":"RX"` 明显大于 1 | **通过** | 当日约 **RX=6950** / 总 16649 |

---

## 四、A 场统计

| 类别 | 通过 | 未测 | 不适用 | 失败 | B 场（本场不计） |
|------|------|------|--------|------|------------------|
| F 透传项（F1/F2/F4/F5/F7/F8/F9） | 5 | 1（F1 预览） | 1（F5） | 0 | F3/F6 |
| App 控件 #1–114（透传子集） | 约 86 | 约 14 | 约 12 | 0 | #68b / #99b |
| N1–N8 | 22 | 1（N3.4 真断） | 2 | 0 | N6.4 / N7.5 |
| 单元测试 | 190 | 0 | 0 | 0 | — |

故意未测（不算失败）：#10 取消区、#36–37 预览层、#60/#104–106 断开重连、#66–67 解绑入口、#76/#82 清空、#91/`#95–98`/`#103` 部分空包与自定义、#101/#102 不发送。

---

## 五、复现命令（A 场保持 UDP 开着）

```powershell
$env:JAVA_HOME = "C:\Users\31470\.jdks\jbr-21.0.11"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
$env:ANDROID_HOME = "C:\Users\31470\AppData\Local\Android\Sdk"
$adb = "$env:ANDROID_HOME\platform-tools\adb.exe"

# 单测（本场已跑）
# Set-Location "d:\work\code field\rescuable-xinglian-weishi"; .\gradlew.bat :app:testDebugUnitTest --no-daemon
# Set-Location "d:\work\code field\rescuesat-ble-sdk"; .\gradlew.bat :rescuesat-ble-core:test --no-daemon

# 当日日志
& $adb -s E6U8QCRWSOPZMJWC shell "run-as com.xinglian.weishi cat files/debug-logs/ble-2026-09-10.jsonl"
```

本场辅助脚本（仅本机联调用）：`rescuable-xinglian-weishi/tools/a_field_ui.py` 及 `a_field_run_*.py`。

**A 场结束：未** `force-stop`、**未** 关中转、**未** 手册 §10。

---

# B 场：非透传（2026-09-10 18:24–18:35 已跑）

开场：A 场结束后 App 开关已显示「已关闭」，但 jsonl **没有** `$TIREL,0`，心跳仍 `$TOLGN,1`（App 本地关、固件 socket 还在）。本场用快捷 `$TIREL,0` 关固件，再开关切换触发 App `close()`。

## 联调结论（B 场）

| 项 | 结果 | 证据 |
|----|------|------|
| G3 / #68 关 / #99b | **通过** | `$TIREL,0` → `$TOREL,0`；开关「已关闭」 |
| F3 | **通过** | App 关开关：`$TILOC,0` t=1789036062743 与 `$TIREL,0` t=1789036062746（差 3 ms） |
| F6 | **通过** | 关后无新 `$TOUDP`；`b10g3` 走卫星 `$TITXT,32805` 无伴随 TOUDP |
| N6.4 | **通过** | 关后 `$TILGN,0` → **`$TOLGN,0`** |
| B-net | **通过** | 关后顶栏「联网中」+ `connection_searching`；`TOSTA net=1` |
| 非透传过线 | **通过** | `b10g3` `$TITXT,32805,5,6231306733` → `$TODST,32805,4` / `$TOACK`（无 TOUDP） |
| G1-C | **通过** | ABCD C1–C7 全 PASS |
| G1-A | **部分失败** | A1 `net=2`、A4 `$TOGAT,1`（关中转后卫星仍附着，不是「从未开中转」冷态） |
| G1-B 脚本 | **B4/B5 失败** | 脚本 `$TITXT,32801` 得 `$TODST,32801,0`，15s 无 TOACK/TODST4 |
| D1–D3 | **通过** | TIADD 开关态可读；中途关无 TOUDP |
| D4 | **失败（测法，非阻塞）** | 复连 `ttMod=1 net=1`，与 09-08 相同 |
| §10 1–4 | **通过** | 已关中转；`adb force-stop`；ABCD D4 已等 32s |
| §10 6 | **未做** | 物理关电源需现场长按 |

App 当前 **已 force-stop**（ABCD 占 GATT）。要继续用 App 请 monkey 启动。

日志：`tt_rescue_stick/Doc/relay_abcd_run_2026-09-10-b.log`，`ble-2026-09-10.jsonl`。

## B 场分项

| # | 项 | 期望 | 结果 | 证据 |
|---|-----|------|------|------|
| G3 | `$TIREL,0` → `$TOREL,0` | 开关已关闭 | **通过** | t=1789036023757 / 关开关再发 t=1789036062746 |
| B-UI #68 关 | 开关关 | 「已关闭」unchecked | **通过** | `udp_relay_status` =「已关闭」 |
| #99b | 快捷 `$TIREL,0` | 发出并回 0 | **通过** | 同上 |
| F3 | 关中转推 `$TILOC,0` | 紧贴 `$TIREL,0` | **通过** | 差 3 ms |
| F6 | 关后无残留 | 无新 `$TOUDP` | **通过** | 末次 TOUDP 在关中转之前 |
| N6.4 | `$TILGN,0` | `$TOLGN,0` | **通过** | t=1789036071091 / 1789036071167 |
| B-net | `net!=2` | 顶栏联网中/搜星 | **通过** | 「202606030046 · 联网中」；随后卫星又回到「已联网」 |
| 排队/非透传发 | `net!=2` 时发 `b10g3` | 能出站或排队后过线 | **通过** | 联网中发出，回 `已联网`；`$TODST,32805,4` 无 TOUDP |
| G1-A | A1–A6 | 未开中转 spec | **3/6 过** | A2/A3/A5/A6 PASS；A1/A4 FAIL（`net=2` / `TOGAT,1`） |
| G1-C | C1–C7 | 关/再开 | **通过** | 7/7 PASS |
| G1-D | D2–D4 | 中途关 / 断 30s | D2/D3 **通过**；D4 **失败（测法）** | `relay_abcd_run_2026-09-10-b.log` |
| §10 | 收尾 | 关中转、force-stop、等 30s | **步骤 1–4 通过**；步骤 6 现场 | App 已停 |

### ABCD 摘要（电脑 BLE，先 force-stop）

```
PASS A2 A3 A5 A6
FAIL A1 (ttMod=1 net=2)  A4 (TOGAT,1)
PASS B1 B2 B3
FAIL B4 B5 (TODST,32801,0 无回执)
PASS C1–C7 D1 D2 D3
FAIL D4 (复连 ttMod=1 net=1，已知测法)
TOTAL 22 FAIL 5
```

---

**计划建立**：2026-09-10  
**A 场填写**：2026-09-10 17:35  
**B 场填写**：2026-09-10 18:35  
**关联**：`ble-2026-09-10.jsonl`（G2 `$TITXT,32801` / B 场 `$TIREL,0`+`$TILOC,0` / `$TOLGN,0` / `b10g3` `$TITXT,32805`）、`tt_rescue_stick/Doc/relay_abcd_run_2026-09-10-b.log`
