---
name: bug-stats
description: 缺陷统计事实源。从禅道缺陷汇总产出结构化 bugStats.json（总数/级别/状态/模块聚合），供钉钉与 Notion 报告只消费不重算。当需要统计项目缺陷分布、生成报告输入数据时使用。
---

# Bug 统计事实源

> 本技能为 qa-agent-report-publish 的共享统计步骤,产出结构化事实对象 `bugStats.json`,钉钉报告与 Notion 报告**只消费、不重算**。

## 输入
来自上游 `zentao-bug-summary` 的输出(MD 或 JSON):
- 项目名 + 日期的 JSON 文件(`{项目}-bugs-{日期}.json`)
- 或已内存传递的缺陷列表对象

## 输出
落盘文件:`output/runtime/reports/{项目}-bugstats-{日期}.json`
```json
{
  "total": 30,
  "byLevel": { "一级": 0, "二级": 3, "三级": 23, "四级": 4 },
  "byStatus": {
    "未关闭": 23,
    "已修复待回归": 7,
    "已延期": 1
  },
  "回归不通过": 4,
  "未关闭列表": [
    { "id": "1234", "级别": "三级", "模块": "电子围栏", "标题": "【电子围栏】进入围栏后未触发报警", "状态": "回归不通过" }
  ],
  "待回归列表": [
    { "id": "1235", "级别": "二级", "模块": "账号管理", "标题": "【账号管理】登录后Token过期", "状态": "已解决" }
  ],
  "byModule": {
    "电子围栏": { "未关闭": 5, "已修复": 2, "延期": 0, "回归不通过": 1 },
    "订阅与通知推送": { "未关闭": 10, "已修复": 0, "延期": 0, "回归不通过": 2 }
  }
}
```

## 统计口径(必须严格遵循)

| 字段 | 计算规则 |
| --- | --- |
| `total` | 输入文件中所有状态(激活/已解决/已延期/已关闭/…)的缺陷总数 |
| `byLevel` | 按缺陷原始 priority 字段聚合(一级/二级/三级/四级) |
| `byStatus.未关闭` | 状态为"激活-待确认"或"激活-已确认(回归不通过)"的缺陷，**不含已延期** |
| `byStatus.已修复待回归` | 状态为"已解决"的缺陷 |
| `byStatus.已延期` | 状态为"已延期"的缺陷，单独一列 |
| `回归不通过` | 状态为"激活-已确认(回归不通过)"的缺陷数 |
| `未关闭列表` | 状态为"激活-待确认"或"激活-已确认"的缺陷(用于第三节列表展示)，不含已延期 |
| `待回归列表` | 状态为"已解决"的缺陷(用于第四节独立列表) |
| `byModule` | 按标题【模块】前缀聚合（**原始前缀，不在本步骤做别名归并**） |

## 模块归属说明（byModule 的 key 是什么）

> ⚠️ **重要**：`bugstats.py` 的 `byModule` key 是禅道缺陷标题 `【...】` 里的**原始前缀**（如"消息通知"、"账号管理"），**不做别名归并**。别名归并（把"消息通知/公众号通知/设备订阅" → "🔔 订阅与通知推送"）发生在下游 `lib/material_context.zentao_module_to_plan`，且**仅在有测试方案资料、渲染完整执行表时**触发。无资料（精简表）时直接展示原始前缀。

**未分类处理**：标题不带 `【】` 前缀的缺陷归入 `未分类` 模块，**不静默丢弃**。`bugstats.py` 的 `self_validate` 会在 `未分类` 缺陷数 > 0 时输出告警（不阻断），提示检查禅道标题格式。

**下游别名映射表**：禅道前缀与测试方案模块名的映射关系见 `lib/report_config.py` 的 `DEFAULT_MODULE_ALIAS`（支持 `--project-config` 项目级覆盖）。本技能不再维护映射表副本——避免与代码漂移。

## 关键约束(禁止违反)
1. **只算一次**:`bugStats` 一旦生成,本发布周期内只读不再重算
2. **禁止重计数**:钉钉报告和 Notion 报告中的每一个数字都必须从 `bugStats` 取值
3. **标题原样**:`未关闭列表` 和 `待回归列表` 中的标题取自禅道 MD 原文,**不许改写**
4. **模块归属为原始前缀**：`byModule` 的 key 是禅道标题 `【...】` 原文前缀，**本步骤不做别名归并**（归并在下游 `material_context` 执行）；标题不带 `【】` 前缀的缺陷归入 `未分类` 并告警

## 执行步骤
1. 读取禅道汇总 JSON 文件(`{项目}-bugs-{日期}.json`)
2. 解析每条缺陷的 `title` / `severity` / `status` 字段
3. 按上述口径聚合生成 `bugStats` 对象
4. 落盘到 `output/runtime/reports/{项目}-bugstats-{日期}.json`
5. 输出统计摘要供 Agent 确认数字
