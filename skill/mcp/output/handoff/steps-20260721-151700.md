前置条件：
1. 对讲群组配置中存在终端备注为空的设备（如设备卡号`2026070201`），可进行多条操作

复现步骤：
1. 在对讲群组配置相关场景中，对备注为空的终端执行配置/查看（当前为多条操作，见截图红字批注）

2. 打开"位置信息"设备详情弹窗，查看"设备卡号""备注"字段（见红框）

3. 对照"设备管理"列表中的"备注名称"及接口返回的`addr`、`remark`字段（见截图右侧 Network）

实际结果：
1. 截图显示：终端备注为空时，界面"备注"/"备注名称"被默认展示为卡号`2026070201`，与"设备卡号"相同

2. 接口返回`addr`与`remark`均为`2026070201`；红字批注：对讲群组配置单中终端备注为空时，默认卡号为备注，当前操作多条

预期结果：
1. 对讲群组配置中终端备注为空时，备注字段保持为空，不得自动回填为设备卡号

2. 接口`remark`在备注未填写时应为空（或 null），不得与`addr`同值；位置信息弹窗与设备列表展示保持一致
