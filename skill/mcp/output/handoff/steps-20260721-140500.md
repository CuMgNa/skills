前置条件：
1. 已登录小程序，进入"聊天消息"页面，会话列表数据不足一屏（如仅`1`条）

复现步骤：
1. 进入"聊天消息"页面（见截图1）

2. 确认当前列表仅有`1`条会话，页面未形成滚动，也未出现明显的"加载更多"过程

3. 观察列表底部文案（见红框）

实际结果：
1. 截图显示：列表仅`1`条数据且未形成滚动时，底部直接展示`没有更多了`

预期结果：
1. 数据不足一屏时，不显示`没有更多了`

2. 数据超过一屏时，用户上拉触底后加载下一页

3. 触底且确认无下一页时，短暂或弱化显示`已经到底啦`（见预期规则截图2）
