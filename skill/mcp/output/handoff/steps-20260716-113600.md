前置条件：
1. 已登录小程序，且可进入"分组管理"页面；对照UI设计稿可见底部添加栏与列表样式

复现步骤：
1. 打开UI设计稿中的"分组管理"页面（见截图左侧）

2. 进入实际实现的"分组管理"页面（见截图右侧）

3. 对比底部添加入口、列表等级标签及右侧操作图标样式

实际结果：
1. 截图显示：实现页底部为圆形"+"悬浮按钮，设计稿为底部输入框`请输入分组名称`+`0/15`+蓝色"添加"按钮

2. 实现页列表缺少设计稿中的`一级`/`二级`标签，右侧操作图标样式与颜色（含删除图标）也与设计稿不一致；红字批注为"分组管理当前实现样式与UI不一致"

预期结果：
1. "分组管理"页面样式与UI设计一致：底部展示输入框`请输入分组名称`、计数`0/15`及"添加"按钮，不使用圆形悬浮添加按钮

2. 列表项展示`一级`/`二级`等级标签，右侧新增/编辑/删除图标样式与颜色与设计稿一致（删除为红色）
