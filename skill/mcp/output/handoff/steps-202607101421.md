前置条件：
1. 当前账号"通知我的紧急联系人"列表存在至少 1 条状态为 接收 的记录

复现步骤：
1. 进入"紧急联系人"页面（见截图1）

2. 切换至"通知我的紧急联系人" Tab

3. 勾选列表中联系人 天地卫通（或任一记录）

4. 查看底部 拒收 与 接收 按钮样式

实际结果：
1. 截图显示：底部 拒收 按钮为白底、红色边框、红色文字；接收 按钮为蓝色实心底、白色文字，两者背景与填充样式明显不一致

预期结果：
1. 同区域操作按钮应使用统一按钮层级样式：接收 为主按钮（实心主题色），拒收 为次按钮（描边或次级色），背景/边框/文字色应符合设计规范 token

2. 拒收 按钮在可用态下背景色应使用次级按钮样式，不得出现白底红边与蓝底主按钮视觉冲突