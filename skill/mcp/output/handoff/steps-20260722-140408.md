前置条件：
1. 管理后台可进入"天通应急套餐明细"列表，且存在含短音/报位变动的明细记录

复现步骤：
1. 进入监控平台管理后台 → "天通应急套餐明细"页面（见截图1）
2. 设备类型选择"全部"，设备ID输入 `2026062601`，点击"查询"
3. 查看列表表头中"变动条数""变动次数"两列文案（见截图红框）

实际结果：
1. 表头仅展示 `变动条数`、`变动次数`，未标明对应短音或报位业务含义
2. 列内存量单位分别为 `条`、`次`，但表头本身无法区分短音变动与报位变动（见红字备注）

预期结果：
1. "变动条数"表头改为 `短音变动条数`，与列内单位 `条` 及短音池口径一致
2. "变动次数"表头改为 `报位变动次数`，与列内单位 `次` 及报位池口径一致
