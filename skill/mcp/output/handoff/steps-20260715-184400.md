前置条件：
1. 已登录监控平台管理后台（如`v2.2.229`），消息管理下存在通知记录数据

复现步骤：
1. 进入"消息管理"-"通知记录"页面（见截图左侧菜单红框）

2. 查看列表中"扣除Web账号""扣除数量""扣除说明"列（见表格红框）

实际结果：
1. 截图显示：多行记录的`扣除Web账号`、`扣除数量`、`扣除说明`列均为空

2. 红字批注：通知记录数据异常，当前对应接口缺少扣除账号字段

预期结果：
1. 通知记录列表接口返回并展示`扣除Web账号`、`扣除数量`、`扣除说明`字段值，有扣除时不得整列为空

2. 无扣除场景时字段展示明确空态或`-`，与有扣除数据行可区分
