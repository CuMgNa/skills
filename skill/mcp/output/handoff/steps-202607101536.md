前置条件：
1. 当前账号"聊天消息-求救群聊"列表存在至少 1 条状态为 `救援中` 的求救群记录，且该群会话内已有最新通信消息

复现步骤：
1. 进入"聊天消息"页面（见截图1）

2. 筛选类型为"求救群聊"

3. 打开求救群 `SOS-2026062603-0004` 会话，查看最新一条消息（如 `sos02`，时间 `15:32`）

4. 返回"聊天消息"列表，查看该群聊项外层消息预览区域

实际结果：
1. 截图显示：求救群 `SOS-2026062603-0004` 会话内最新消息为 `sos02`（`15:32`），但列表项外层消息预览区域为空，未展示该条最新消息；时间戳已更新为 `15:32`

预期结果：
1. 求救群聊列表项外层应在收到/发送消息后 ≤3s 内展示最新一条通信消息预览（文本消息展示原文，语音展示 `[语音]` 等约定占位）

2. 列表项外层消息预览、时间戳与群内最新消息内容保持一致；如 `sos02` 为最新消息，外层应展示 `sos02`
