前置条件：
1. 已登录监控平台管理后台（如`v2.2.236`），进入"入库管理"-"分组管理"，并可打开分组下的"入库设备列表"弹窗

复现步骤：
1. 进入"入库管理"-"分组管理"页面

2. 打开某分组的"入库设备列表"弹窗（见截图，如`tmn萌鸡小队!`）

3. 点击设备类型筛选下拉框，展开设备类型列表（见红箭头指向处）

4. 尝试通过关键词快速定位目标设备类型

实际结果：
1. 截图显示：设备类型下拉仅支持滚动点选，选项较多（如`4G太阳能报位设备(二代)`、`北二车载`、`北三手持机`等）

2. 红字批注：当前设备类型太多，需增加关键词搜索列表匹配功能；现状无关键词搜索入口

预期结果：
1. 设备类型下拉支持关键词搜索，输入关键字后列表仅展示匹配项

2. 匹配规则为包含匹配（名称包含输入关键字即可命中），清空关键字后恢复展示全部类型（含`所有`）
