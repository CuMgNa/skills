前置条件：
1. 微信小程序与星联卫士APP处于同一群组消息链路，小程序端可录制并发送语音，APP端可接收
2. 小程序语音录制最长时长为 `10秒`

复现步骤：
1. 在微信小程序群聊中长按“按住 说话”录制语音直至达到最高时长，松手后消息自动发送（见截图1左侧）
2. 切换至星联卫士APP“消息”页，等待接收该语音消息（见截图1右侧）
3. 对比小程序与 APP 两端该批语音消息的时长展示

实际结果：
1. 截图显示：同一批语音消息，小程序端时长展示为 `10''`、`10''`、`10''`、`8''`，APP端对应展示为 `9秒`、`9秒`、`9秒`、`7秒`（红框标注处）
2. 据用户描述：APP接收的语音时长与小程序不一致，统一少 `1秒`

预期结果：
1. 同一语音消息APP端展示时长与小程序端一致，时长偏差为 `0秒`（如小程序展示 `10秒`，APP亦展示 `10秒`）
2. 两端语音时长均以音频文件实际时长为准，展示差异消息数为 `0`
