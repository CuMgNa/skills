前置条件：
1. 系统时区已切换为 `Australia/Darwin`。

复现步骤：
1. 进入“Group Management”页面。
2. 新建一个分组并记录实际操作时间。
3. 查看新增分组的“Creation Time”。

实际结果：
1. 新增分组的创建时间显示为 `2026-09-08 17:22:20`，截图中的实际操作时间约为 `2026-09-08 15:52:30`，相差约 `1小时30分钟`。

预期结果：
1. 创建时间必须基于统一 UTC 时间保存，并按照当前选择的 `Australia/Darwin`时区转换展示。
2. 创建时间与实际创建时刻对应的绝对时间偏差不得超过 `1分钟`，不得发生重复时区转换。
