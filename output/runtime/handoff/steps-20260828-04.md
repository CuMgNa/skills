前置条件：
1. 进入测试环境的英文页面，页面右上角存在对应功能图标。

复现步骤：
1. 进入 `Console` 页面。

2. 将鼠标悬浮到截图标注的测试环境图标。

3. 查看图标提示文案。

实际结果：
1. 测试环境图标提示仍显示 `北斗通信`，未按标注要求显示 `聊天消息`。

预期结果：
1. 测试环境下该图标的提示文案应显示为已确认的英文或目标文案 `聊天消息`，不得继续显示 `北斗通信`。
