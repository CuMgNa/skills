前置条件：
1. 系统语言已切换为 `English`。

2. 已打开 Subscribe 设备选择弹窗。

复现步骤：
1. 打开 Subscribe 弹窗，见截图。

2. 查看左侧 `Cancel Select All` 按钮。

实际结果：
1. `Cancel Select All` 文案超出按钮可用宽度，与相邻按钮区域拥挤。

预期结果：
1. 按钮文案与按钮边界的重叠面积应为 `0px²`。

2. 完整文案与左右边界之间应分别至少保留 `8px` 间距。
