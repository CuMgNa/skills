前置条件：
1. 已配置系统邮件通知，收件箱可收到报警、聊天、上线、绑定邮箱验证码、报警统计等邮件

复现步骤：
1. 触发系统邮件通知（报警、聊天、上线、绑定邮箱验证码、报警统计等）

2. 进入邮箱"收件箱"页面（见截图1）

3. 查看列表中各邮件的发送方显示名

实际结果：
1. 截图显示：今天收到的 8 封系统邮件发送方均为 `【广州磐钴智能】`，覆盖 `T-报警通知`、`T-聊天消息通知`、`T-上线通知`、`绑定邮箱验证码`、`报警统计通知`、`T-报警统计通知`

2. 红字标注原文：邮件的发送方没有改成对应的【天翼物联】

预期结果：
1. 邮件发送方显示名必须逐字为 `【天翼物联】`（含【】，不得多字、少字、错字）

2. 上述通知类型的发送方均须为 `【天翼物联】`，不得继续显示 `【广州磐钴智能】`
