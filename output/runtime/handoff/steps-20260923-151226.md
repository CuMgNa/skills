前置条件：
1. APP通过蓝牙连接星联卫士终端（如 `202606030043`），状态栏展示“已联网”
2. 发送队列中存在正在发送的消息（如图片处于“发送中”）

复现步骤：
1. APP通过蓝牙连接终端设备，确认状态栏展示“已联网”（见截图1，红圈标注处）
2. 在APP端发送一张图片，等待其进入“发送中”状态
3. 继续发送一条文本消息
4. 观察该文本消息的状态提示

实际结果：
1. 截图显示：设备状态为“已联网”（红圈标注处），文本消息“我在这里一切正常”状态展示为“等联网后发出”（红圈标注处）
2. 据用户描述：该文本消息实际处于排队等待发送状态（队列中图片正在发送），提示与实际联网及排队状态不符

预期结果：
1. 设备已联网时，队列等待中的消息状态展示为排队等待类文案（如“排队等待发送”），不得展示“等联网后发出”
2. 消息状态文案与联网状态、队列实际状态一致，误提示消息数为 `0`
