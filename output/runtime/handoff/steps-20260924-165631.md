前置条件：
1. APP已通过蓝牙连接星联卫士终端 A（如 `202606030043`，已联网）
2. APP“消息”页可正常发送图片消息

复现步骤：
1. 在 APP“消息”页发送图片 A，等待图片 A 进入“发送中”状态
2. 在图片 A 发送过程中取消发送
3. 继续发送图片 B（见截图1）
4. 观察图片 B 的发送状态，等待至消息处理结束

实际结果：
1. 截图显示：图片 A 状态为“发送失败 · 已取消”，图片 B 状态停留在“等待发送”
2. 据用户描述：图片 B 长时间未进入发送流程，最终因发送超时失败

预期结果：
1. 取消图片 A 后立即释放发送队列及占用资源，取消操作完成后队列锁定残留数为 `0`
2. 图片 B 在加入空闲发送队列后 `3秒` 内进入“发送中”状态，不得持续停留在“等待发送”直至超时
