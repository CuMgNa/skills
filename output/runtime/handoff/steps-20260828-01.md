前置条件：
1. 进入英文环境，设备列表中存在 `Tiantong Rescue Stick` 类型设备。

复现步骤：
1. 进入 `Device` 页面。

2. 查看设备列表中的 `Device Type` 字段。

实际结果：
1. `Device Type` 字段显示为 `Tiantong Rescue Stick`，与截图标注要求的 `星际卫士` 文案不一致。

预期结果：
1. `Device Type` 字段应按已确认词汇表统一显示指定文案 `星际卫士`，同一设备类型在列表及其他页面中的文案逐字一致。
