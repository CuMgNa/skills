前置条件：
1. 系统语言已切换为英文，并已从“Batch Storage”下载批量入库模板。

复现步骤：
1. 进入“Group Management”页面并打开“Stored Device List”。
2. 点击“Batch Storage”下载 Excel 模板。
3. 打开模板，点击 `Device Type`列中的下拉按钮。
4. 观察设备类型选项是否展开。

实际结果：
1. Excel 模板的 `Device Type`单元格存在下拉标识，但点击后无法展开设备类型选项。

预期结果：
1. 点击 `Device Type`下拉按钮后，应在 `1秒`内显示可选设备类型列表。
2. 设备类型必须从模板预设枚举中选择，不在枚举中的值应在导入时被拦截并精确提示所在行。
