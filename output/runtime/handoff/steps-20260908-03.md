前置条件：
1. 分组管理列表中存在至少 1 条分组记录。

复现步骤：
1. 进入“分组管理”页面。
2. 查看列表中的“状态”列及各行状态值。

实际结果：
1. 列表展示“状态”列，截图内全部记录均固定显示为 `正常`，该列没有有效区分作用。

预期结果：
1. 当前版本不支持分组状态变化时，列表应隐藏“状态”列，列表列数减少 `1列`。
2. 如产品保留该列，则状态值必须至少包含 `2种`可区分的业务状态，并与实际分组状态一致。
