前置条件：
1. 设备可触发带位置的短信通知（`advanceH5SmsTemplate`），收件手机可收到【天翼物联】短信

复现步骤：
1. 触发带经纬度的短信通知（落水报警、微信消息位置信息等）

2. 打开手机短信，查看正文中"位置信息"字段（见截图1左侧）

3. 对照模板 `advanceH5SmsTemplate` 样例中的度分秒格式（见截图1右侧）

实际结果：
1. 截图显示：落水报警短信位置为 `北纬39'54'59`、`东经115'23'50`，度分秒均用 `'`，缺少 `°` 与 `"`

2. 同图另一条微信消息位置短信为 `北纬('0'0)`、`东经('0'0)`，出现括号与引号拼接。红字标注原文：经纬度的度分秒定界符使用错误

预期结果：
1. 经纬度度分秒定界符必须为 `度°分'秒"`，与样例一致，如 `北纬23°16'19"`、`东经113°27'30"`

2. 不得将度符号写成 `'`，不得输出 `北纬('0'0)`、`东经('0'0)` 这类括号+引号拼接
