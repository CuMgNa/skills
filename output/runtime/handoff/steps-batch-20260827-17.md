前置条件：
1. 系统语言已切换为 `English`。

2. Geofence 列表中存在至少一条围栏数据。

复现步骤：
1. 进入 Geofence 列表，见截图。

2. 查看列表最右侧操作列表头。

实际结果：
1. 操作列表头被截断显示为 `Act...`，无法直接识别完整字段含义。

预期结果：
1. 操作列表头应完整显示为 `Actions`，省略字符数量应为 `0`。

2. 当空间不足时，应调整列宽或提供可显示完整文案的悬浮提示。
