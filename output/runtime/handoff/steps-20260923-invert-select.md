前置条件：
1. APP 已登录且已绑定设备，可进入“指令调试”页面

复现步骤：
1. 进入星联卫士APP“指令调试”页面，打开交互日志筛选的“选择指令”弹窗（见截图1）

2. 点击弹窗底部“反选”按钮

3. 单击任一指令（如 `TOGT`）的复选框，尝试单个勾选或取消勾选

实际结果：
1. 截图显示：点击“反选”后，单击指令复选框（如 `TOGT` 行）无响应，无法对单个指令进行勾选或取消勾选

预期结果：
1. 点击“反选”后，单击任一指令复选框 `≤1s` 内完成勾选/取消状态切换

2. “反选”仅一次性取反全部指令勾选状态，不改变后续单个复选框的可交互性