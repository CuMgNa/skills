前置条件：
1. 打开大屏监控页

复现步骤：
1. 进入大屏监控页（见截图1）

2. 点击右上角设置图标打开 Settings 弹窗

3. 查看 Update Interval 标签与输入框

实际结果：
1. 截图显示：标签 `Update Interval` 折行为 `Interva` 与 `l`，输入框内数值 `5` 与单位 `s` 换行错位

预期结果：
1. 标签、输入框、单位同行完整展示，`Update Interval` 不折行

2. 数值与单位 `s` 同行相邻展示，无错位
