前置条件：
1. APP通过蓝牙连接星联卫士终端（如 `202606030043`，已联网）
2. 发送队列中存在正在发送的消息（如图片），队列非空闲

复现步骤：
1. APP通过蓝牙连接终端设备（见截图1）
2. 在APP端发送一张图片，等待其进入“发送中”状态
3. 同时使用星联卫士终端录制并发送一条语音
4. 观察 APP“消息”页该语音消息的状态展示

实际结果：
1. 截图显示：语音消息状态展示为“发送中 3/3”（红圈标注处），同页图片消息状态为“发送中 8/30”
2. 据用户描述：该语音消息实际处于等待发送队列中，尚未开始发送，状态误显示为“发送中”

预期结果：
1. 语音消息处于等待发送队列时，状态展示为等待发送类文案（如“等联网后发出”），不得展示“发送中”
2. 消息状态与队列实际进度一致，状态误展示的消息数为 `0`
