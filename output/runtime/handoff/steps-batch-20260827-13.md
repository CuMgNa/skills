前置条件：
1. 系统语言已切换为 `English`。

2. 当前账号具有创建对讲群权限。

复现步骤：
1. 点击创建对讲群按钮，见截图。

2. 查看创建弹窗中的标题、字段、占位符和按钮。

实际结果：
1. 弹窗显示 `创建对讲群`、`群聊名称`、`长度不得超过15字符`、`确认` 等中文文案。

预期结果：
1. 系统语言为 `English` 时，创建群聊弹窗中的中文文案数量应为 `0`。

2. 标题、字段名、占位符、长度规则和确认按钮应使用英文语言资源。
