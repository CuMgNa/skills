前置条件：
1. 已打开 `PN06 Command Dispatch` 弹窗。

复现步骤：
1. 进入 Batch Command Dispatch 页面，见截图。

2. 查看 `Modify Report Frequency` 及其模式选项。

实际结果：
1. `Modify Report Frequency` 标签与右侧模式控件间距过小，红框区域文字和控件边界拥挤。

预期结果：
1. 标签与右侧控件的重叠面积应为 `0px²`。

2. 标签、单选控件和输入框之间应至少保留 `8px` 间距。
