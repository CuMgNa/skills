前置条件：
1. 设备名称已由“天通救援棒”变更为“星联卫士”，且存在可编辑的星联卫士入库记录。

复现步骤：
1. 进入“设备入库”页面。
2. 打开设备名称为“星联卫士”的入库信息编辑弹窗。
3. 清空 `ICCID`字段并触发必填校验。

实际结果：
1. 当前设备名称为 `星联卫士`，但校验提示仍显示 `天通救援棒的 ICCID 不能为空`。

预期结果：
1. 校验提示必须与当前设备名称一致，逐字显示为 `星联卫士的 ICCID 不能为空`。
2. 设备名称变更后，页面标题、字段提示和校验文案中的旧名称 `天通救援棒`出现次数必须为 `0`。
