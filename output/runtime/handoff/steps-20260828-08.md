前置条件：
1. 进入英文环境，存在设备告警记录并打开告警弹窗。

复现步骤：
1. 进入 `Console` 页面。

2. 打开设备告警详情弹窗。

3. 查看 `Alarm Type` 字段。

实际结果：
1. `Alarm Type` 字段显示为 `SOS报警`，其中 `报警` 未翻译为对应英文。

预期结果：
1. 英文环境下 `Alarm Type` 字段应完整显示已审核的英文文案，例如统一使用 `SOS Alarm`，不得混用中文 `SOS报警`。
