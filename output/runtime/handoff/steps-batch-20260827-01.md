前置条件：
1. 系统语言已切换为 `English`。

2. 已进入 Console 的对讲群聊窗口。

复现步骤：
1. 打开对讲群聊窗口，见截图。

2. 查看页签、剩余定位次数、会话状态及底部按钮。

实际结果：
1. 英文界面仍显示 `对讲群聊`、`0次`、`已结束`、`全部已读`、`创建对讲群` 等中文文案。

预期结果：
1. 系统语言为 `English` 时，对讲群聊区域中文字文案数量应为 `0`。

2. 页签、次数单位、状态及按钮均应使用对应英文语言资源。
