前置条件：
1. 求救群聊中同时存在文本消息和语音消息。

2. 两类消息均已被群内成员读取。

复现步骤：
1. 进入求救群聊，见截图。

2. 对比文本消息和语音消息下方的 `Read in Group` 状态。

实际结果：
1. 文本消息的 `Read in Group` 使用蓝绿色，语音消息的 `Read in Group` 使用绿色，相同状态的颜色不一致。

预期结果：
1. 相同 `Read in Group` 状态应使用同一颜色值，颜色差异数量应为 `0`。

2. 文本和语音消息的已读状态样式应保持一致。
