前置条件：
1. 已准备分组名称长度超过 20 个字符的 Excel 批量导入文件。

复现步骤：
1. 进入“分组管理”页面，使用 Excel 批量导入分组。
2. 打开导入成功的分组编辑弹窗。
3. 查看“分组名称”字段的字符计数和校验状态。

实际结果：
1. 超长分组名称成功写入，编辑弹窗显示字符计数为 `40/20`，后端未限制分组名称最多 20 个字符。

预期结果：
1. Excel 批量导入与页面编辑使用同一校验规则，`分组名称`长度范围为 `1～20个字符`。
2. 导入值超过 20 个字符时必须拦截该行，并提示“分组名称不能超过20个字符”，不得写入数据库。
