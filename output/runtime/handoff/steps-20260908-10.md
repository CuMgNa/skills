前置条件：
1. 实时位置列表中存在跨日期或不同时间更新的设备记录。

复现步骤：
1. 进入“Data Overview”页面。
2. 查看“实时位置动态”列表。
3. 观察各设备记录的“更新时间”。

实际结果：
1. 更新时间仅显示 `18:01:08`、`16:18:52`、`15:59:45`等时分秒信息，缺少年、月、日。

预期结果：
1. 更新时间必须统一采用 `YYYY-MM-DD HH:mm:ss`格式展示。
2. 每条记录必须同时显示日期和时间，缺失日期部分的记录数量必须为 `0`。
