前置条件：
1. 系统语言已切换为 `English`。

2. 已打开 Add Electronic Fence 弹窗。

复现步骤：
1. 进入 Geofence 页面，见截图。

2. 打开新增电子围栏弹窗。

3. 查看坐标字段和设备选择按钮区域。

实际结果：
1. `Center Coordinates` 被拆成两行显示为 `Center Coordinat` 和 `es`。

2. `Device Selection` 与 `Fence Data Import` 按钮纵向排列且未与字段保持统一对齐。

预期结果：
1. `Center Coordinates` 应在一行完整显示，换行次数应为 `0`。

2. 同组操作按钮应在同一行对齐，按钮基线偏差应不超过 `1px`。

3. 字段与按钮之间应至少保留 `8px` 间距。
