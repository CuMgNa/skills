前置条件：
1. 已打开 `PN07 Command Dispatch` 弹窗。

复现步骤：
1. 进入 Batch Command Dispatch 页面，见截图。

2. 查看 `Modify Report Frequency` 和 `Modify Tilt Arm Angle` 两行。

实际结果：
1. `Query` 控件紧贴字段名称，形成 `Modify Report FrequencyQuery` 和 `Modify Tilt Arm AngleQuery` 的重叠展示。

预期结果：
1. 字段名称与右侧控件的重叠面积应为 `0px²`。

2. 字段名称与相邻控件之间应至少保留 `8px` 水平间距。
