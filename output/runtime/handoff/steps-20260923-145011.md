前置条件：
1. APP已通过蓝牙连接终端设备（如 `202606030043`，已联网）
2. 终端可录制并发送语音消息

复现步骤：
1. APP通过蓝牙连接终端设备（见截图1）
2. 在终端侧录制一段时长大于 1 秒的语音并发送（如 5 秒）
3. 观察 APP“消息”页该语音气泡的时长展示变化

实际结果：
1. 截图显示：语音气泡当前展示为 `5秒终端`，状态为发送成功（红圈标注处）
2. 据用户描述：消息接收时气泡时长默认显示为 `1秒`，随后才更新为实际时长，语音时长展示不准确

预期结果：
1. 语音气泡接收展示时直接显示实际时长（如 `5秒`），不得先默认显示 `1秒` 再跳变更新
2. 气泡展示时长与语音实际时长一致，偏差为 `0秒`
