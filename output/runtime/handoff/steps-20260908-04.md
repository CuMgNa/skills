前置条件：
1. 系统语言已切换为英文，Arrears Bill 列表中存在可导出的数据。

复现步骤：
1. 进入“Arrears Bill”页面。
2. 点击“Export”导出欠费数据。
3. 打开导出的 Excel 文件并查看表头。

实际结果：
1. 英文环境导出的 Excel 表头仍显示 `账号名称`、`手机号码`、`语音欠费总个数`、`定位欠费总个数`、`欠费状态`等中文文案。

预期结果：
1. 英文环境导出的全部 Excel 表头必须使用英文语言包，中文字符数量必须为 `0`。
2. Excel 表头的字段名称、排列顺序和业务含义必须与英文页面列表逐列一致。
