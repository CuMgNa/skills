前置条件：
1. Stored Device List 中存在 `Device Type`或`Storage Time`超过列宽的记录。

复现步骤：
1. 进入“Group Management”页面并打开“Stored Device List”。
2. 查看以省略号截断的 `Device Type`和`Storage Time`内容。
3. 将鼠标悬浮在截断文本上方。

实际结果：
1. `Tiantong Resc...`和`2026-07-27 15...`等内容被省略号截断，鼠标悬浮后未显示完整文本。

预期结果：
1. 鼠标悬浮在省略文本上方后，应在 `500毫秒`内显示包含完整原文的 Tooltip。
2. Tooltip 内容必须完整且与单元格原始值逐字一致，不得继续截断或超出可视区域。
