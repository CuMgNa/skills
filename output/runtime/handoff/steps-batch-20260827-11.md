前置条件：
1. 系统语言已切换为 `English`。

2. 已打开对讲群聊邀请详情窗口。

复现步骤：
1. 打开邀请详情，见截图。

2. 查看状态页签和表格列名。

实际结果：
1. 弹窗仍显示 `全部`、`待处理`、`已同意`、`已拒绝`、`已失效`、`邀请人`、`邀请设备`、`群名称` 等中文文案。

预期结果：
1. 系统语言为 `English` 时，邀请详情弹窗中的中文文案数量应为 `0`。

2. 状态页签、表头、分页及操作列应统一使用英文语言资源。
