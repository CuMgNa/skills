前置条件：
1. 已进入 Console 消息窗口。

2. 当前消息类型已切换为 `调度组`。

复现步骤：
1. 打开消息类型下拉框，见截图。

2. 查看当前值及下拉选项。

实际结果：
1. 下拉框当前显示 `调度组`，但展开后仅显示 `终端消息`、`对讲群聊`、`求救群聊`，当前选项未出现在列表中。

预期结果：
1. 下拉框当前值必须在候选列表中存在且仅存在 `1` 次。

2. 展开下拉框后，当前值应具有明确选中状态。
