前置条件：
1. 个人账号列表中存在账号 `user1752216001906`。

2. 当前账号具有编辑个人账号权限。

复现步骤：
1. 进入个人账号列表，见截图。

2. 点击对应账号的编辑操作。

3. 不修改账号字段，点击 `确定`。

实际结果：
1. 账号输入框为不可编辑状态且已有值 `user1752216001906`，提交时仍提示 `请输入账号`。

2. 表单被必填校验阻断，无法继续提交。

预期结果：
1. 不可编辑且已有值的账号字段应通过必填校验，不应出现 `请输入账号`。

2. 账号字段存在非空值时，必填校验错误数量应为 `0`。
