前置条件：
1. 微信小程序已进入与终端 `202606030043` 的聊天页，可使用“按住 说话”录音

复现步骤：
1. 进入小程序聊天页（见截图1）
2. 极短按住“按住 说话”后立即松手，录制时长接近 `0秒`
3. 观察语音消息是否发送及气泡时长展示

实际结果：
1. 截图显示：聊天记录中存在时长为 `0''` 的语音消息（红框标注处，17:45），已正常发送且显示终端已读
2. 据用户描述：小程序录音可为0秒并发送

预期结果：
1. 录音时长不足 `1秒` 时不发送该语音，并给出引导提示（如“说话时间太短”），发送的 `0秒` 语音消息数为 `0`
2. 已发送语音消息的时长均大于等于 `1秒`
