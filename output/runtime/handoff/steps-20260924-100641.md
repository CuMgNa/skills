前置条件：
1. APP已进入“消息”页，存在接收方（设备）消息与发送方消息记录

复现步骤：
1. 进入“消息”页（见截图1右侧）
2. 查看接收方消息气泡与左侧头像之间的间距
3. 与微信消息列表的气泡头像间距对比（见截图1左侧）

实际结果：
1. 截图显示：接收方消息气泡边缘与头像区域过于贴近，间距明显小于微信同场景布局（红框标注处），视觉拥挤
2. 据用户描述：接收发消息气泡与头像之间的布局间距不合理，参考微信间距

预期结果：
1. 接收方消息气泡与头像之间保持合理间距，参照微信消息布局，间距不小于 `8dp` 且全列表统一
2. 发送方与接收方气泡与各自头像间距保持一致，间距不一致处数为 `0`
