前置条件：
1. 终端消息列表中存在至少一条当天产生的消息。

复现步骤：
1. 进入终端消息页面，见截图。

2. 选中时间显示为 `10:59` 的会话。

3. 查看右侧对应消息的时间。

实际结果：
1. 左侧列表显示时间为 `10:59`，右侧详情显示为 `2026-08-05 10:59:06`，同一窗口内时间格式不一致。

预期结果：
1. 同一会话窗口内的时间格式应保持一致，统一采用 `YYYY-MM-DD HH:mm:ss`。

2. 同一消息在列表与详情中的时间值误差应为 `0秒`。
