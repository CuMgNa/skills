前置条件：
1. 账号已登录位置监控平台，系统语言可切换为 `English`，且可打开"Dispatch / 对讲群聊"聊天室并存在群聊记录

复现步骤：
1. 进入监控相关页面并打开"Dispatch"弹窗（见截图1）

2. 将系统语言切换为 `English`（见截图右上角红框）

3. 进入左侧 Tab"对讲群聊"区域，查看顶部额度文案、消息状态标签与底部操作按钮

实际结果：
1. 系统语言为 `English` 时，聊天室仍残留多处中文，截图可见：`对讲群聊`、`Remaining Positioning: 0 次` 中的 `次`、`已结束`、`已退出`、`全部已读`、`创建对讲群`

预期结果：
1. 语言为 `English` 时，上述文案须全部为对应英文（不得出现 `次`、`对讲群聊`、`已结束`、`已退出`、`全部已读`、`创建对讲群` 等中文）

2. 多语言缺失时不得回退显示中文原文，须与当前语言包逐字一致（含大小写与空格）
