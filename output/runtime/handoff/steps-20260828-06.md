前置条件：
1. 进入英文环境的 `Dispatch` 页面，存在 SOS 消息记录。

复现步骤：
1. 进入 `Dispatch` 页面。

2. 切换至 `SOS` 消息。

3. 查看左侧消息列表和右侧消息详情内容。

实际结果：
1. 切换至 `SOS` 消息后，消息内容由英文变为中文，且右侧消息内容显示不完整。

预期结果：
1. 英文环境下，切换至 `SOS` 消息后，消息标题、正文及详情内容应保持统一英文，内容完整展示，不得因切换产生语言变化或缺失。
