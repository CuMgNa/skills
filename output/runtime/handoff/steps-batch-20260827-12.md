前置条件：
1. 系统语言已切换为 `English`。

2. 当前对讲群中存在至少一个套餐设备。

复现步骤：
1. 打开对讲群聊，见截图。

2. 打开套餐余量弹窗。

3. 查看弹窗标题、字段和按钮。

实际结果：
1. 套餐余量弹窗中的标题、`短信剩余`、`定位剩余` 及底部按钮仍显示中文。

预期结果：
1. 系统语言为 `English` 时，套餐余量弹窗中的中文文案数量应为 `0`。

2. 标题、余量字段、单位和操作按钮应统一使用英文语言资源。
