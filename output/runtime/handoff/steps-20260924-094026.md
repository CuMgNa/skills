前置条件：
1. 终端账户余额（豆数）未用完但处于不足状态（如当前豆数 `9`）
2. APP已通过蓝牙连接星联卫士终端（如 `202606030043`，已联网）

复现步骤：
1. 查看小程序“我的信息”页，确认当前豆数为 `9`（见截图1左侧，红框标注处）
2. 在星联卫士APP“消息”页发送一条文本消息（见截图1右侧）
3. 观察消息发送状态，并在终端上直接发送消息进行对比

实际结果：
1. 截图显示：豆数剩余 `9`（未用完）时，APP发送的多条消息状态均为“发送失败 · 余额不足”（红框标注处）
2. 据用户描述：同一账户下使用终端仍可正常发送消息，仅APP端发送被拦截

预期结果：
1. 余额不足（豆数 > `0`）仅为预警状态，APP正常发送消息；拦截条件为余额用完（豆数 = `0`）
2. 余额不足时APP发送行为与终端侧保持一致，不拦截发送，可补充余额预警提示（如提醒充值）
