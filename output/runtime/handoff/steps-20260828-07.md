前置条件：
1. 进入英文环境的 `Dispatch` 页面，左侧消息列表存在英文消息预览。

复现步骤：
1. 进入 `Dispatch` 页面。

2. 查看左侧消息列表中的 SOS 消息。

3. 观察消息预览内容。

实际结果：
1. 左侧消息预览显示为截断文案 `In danger, falling water ala...`，消息内容未完整展示。

预期结果：
1. 消息预览应按界面宽度完整显示已审核的英文文案；若确需省略，应提供可识别的完整内容入口，且不得截断关键告警含义。
